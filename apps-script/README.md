# Apps Script – brevlådan för bokningsmodulen

Scriptet är den enda komponent som läser CJ:s kalendrar och Outlook-ICS, räknar restid, tar emot bokningar,
skriver i kalendern "Bokningar" och i brevlådefilerna i Drive. Det körs som CJ ("Kör som: Jag") och nås
anonymt av bokningssidan och Pipeline-appen via `BOK_SCRIPT_URL`. Specifikationen är sanningen:
`02 Projects/Pipeline/[C] Bokningsmodul - specifikation steg 1.md` (avsnitt 4, 5 och 9).

## Filer

| Fil | Innehåll |
|---|---|
| `appsscript.json` | Manifest: tidszon Europe/Stockholm, V8, explicita OAuth-scopes (sex st: `drive`, `calendar`, `script.external_request`, `script.send_mail`, `script.scriptapp`, `userinfo.email` – den sista krävs för `Session.getEffectiveUser().getEmail()` i filkontrollen, spec 4.2), Calendar advanced service v3, webapp "kör som den som distribuerar" + åtkomst "Alla" (`ANYONE_ANONYMOUS`). Ändras scopes måste CJ godkänna om scriptet. |
| `Code.gs` | Kärnan: Script Properties, filhantering (exakt fyra fil-id:n sedan version 13 – kontofilen är valfri), `doPost`/`doGet` med JSON-kuvert och felkoder, autentisering (session `s` via Konton.gs, bokarkod `k` bara för CJ-bokare sedan version 13, adminnyckel i konstant tid), anrops- och handlingsgränser, reservationer (CacheService), `book` med idempotens och kompenserande borttagning, kalenderskrivning, notismejl, `hello`/`ping`/`geocode`/`release`, admin-endpoints `setup`/`config-push`/`calendars-list`/`inbox-list`/`ack`/`reject` (M3) och `calendar-preview`/`rebook`/`cancel` (M4) och `purge` (M5; se "Admin-endpoints" nedan), bokar-endpoints `egen-rebook`/`egen-cancel`/`egen-update` (steg 2a, bokarens självservice – se "Bokar-endpoints egen-*" nedan), `adressforslag` (steg 2b, adress-autocomplete via Places – se "Adressförslag" nedan) och `placeId` bredvid adress i `availability`/`reserve`/`book`/`geocode`/`egen-update`/`egen-rebook`/`rebook`, `install()`, `dailyMaintenance()` (M5: gallring, avstämning – se "dailyMaintenance" nedan) och `refreshIcsCache()` (version 7: ICS-värmare var 10:e minut, inkorg i CacheService, Drive-filmemo – se "Version 7" nedan), blocksynken `syncBlock_` + admin-endpointen `block-sync` (version 11: restid- och marginalblock som händelser i den egna kalendern "Pipeline – restid" – se "Version 11" nedan). |
| `Calendar.gs` | Kalenderläsning: `readBusy(fran, till)`, ICS-parser, `mergeBusy`, `applyIgnore` (Ignorera/Räkna **och** manuell restidsklassning/rättad adress per händelse ur `KALENDER_IGNORERA` – version 9, se nedan), `buildBusyList(from, to)` (ignorerade poster behålls med `ignorerad:true` – Kalenderkoll visar dem gråade; inkorgsposter får titeln `Bokning` utan kundnamn), `finalizeBusy` + `kalMarginal_` (version 10: `cooldownMin` = effektiv buffert efter händelsen = max(cooldown, marginal fysiskt/online)), komprimerad/chunkad ICS-cache och långsam-fallback (M5, se "ICS-cache" nedan), `listGoogleEventsAvstamning_`/`listGoogleEventsByBokningId_` (avstämning/purge – avgränsade `Events.list`). Version 11 (K6): `readBusy` hoppar över blockkalendern (`BLOCK_KALENDER_ID`) oavsett läge och `normalizeGoogleEvent` släpper aldrig igenom händelser med `private.pipelineBlock === '1'`. |
| `Konton.gs` | **Bokarkonton (steg 3, version 13, spec `[C] Bokningsmodul - specifikation steg 3 bokarkonton.md`):** kontofilen `telexia-bokning-konton.json` (läs/skriv med CacheService-kopia som inkorgen), lösenordshash (itererad HMAC-SHA256 + salt + `LOSEN_PEPPER`), sessioner (HMAC-signerad statslös token `s` som alla bokar-endpoints tar i stället för `k`), tillåtelselistan `config.domaner` (`matchaLista_`: hel adress före domän) och `effectiveBokare_` (konto + appens bokarpost), endpoints `konto-registrera`/`konto-verifiera`/`konto-logga-in`/`konto-glomt`/`konto-aterstall`/`konto-byt-losenord`/`konto-profil` (bokare) och `konton-list`/`konto-radera` (admin), konto-mail (verifiering, återställning, "konto finns redan", notis till CJ om ny bokare), gallring av overifierade konton (`gallraKonton_` i `dailyMaintenance`), `ping.konton`, `runKontoTests()`. Se "Version 13" nedan. |
| `Availability.gs` | Tillgänglighet och restid: `computeAvailability(req)`, `dayPlan`, `placeTravelDelar`/`placeTravel` (version 10: resan delad runt platslösa händelser), `travelMinutesRaw` (version 10: Googles körtid utan påslag; `travelWithMargin` finns kvar som alias), `luckBuffertMin`, `geocodeAddress(adress, { placeId })` (steg 2b: `place_id`-uppslag när förslaget valts), `hamtaAdressforslag(q, sessionToken)` + `placesAutocomplete_` (steg 2b: Places API (New) Autocomplete – det enda anropet mot `places.googleapis.com`) + editorfunktionen `debugPlaces(q)` (deploy-verifiering), restid via Distance Matrix med cache (`travelSecondsForPairs_` – delas av `availability` och `calendar-preview`), svenska röda dagar. Steg 2c: områdesetikett `omrade = { stad, stadsdel, etikett }` ur Geocoding `address_components` (sparas i cache-filens geokodposter och CacheService), `previewResor` (restid mellan dagens ankare för Kalenderkoll) och `backfillOmrade_` (nattlig omgeokodning av poster utan `omrade`). |

Alla fyra `.gs`-filer klistras in i samma Apps Script-projekt (filnamn spelar ingen roll för Apps Script, men behåll dem för läsbarhet). Inga hemligheter finns i koden – Maps-nyckel, adminnyckel och fil-id:n ligger enbart i Script Properties.

## Script Properties (Projektinställningar › Script Properties)

| Nyckel | Sätts av | Betydelse |
|---|---|---|
| `ADMIN_KEY` | **CJ, för hand** – Anslut-guiden genererar nyckeln och visar den med "Kopiera"; klistra in här och klicka "Fortsätt" (spec 4.5, 10.1 steg 4b). Scriptet sätter aldrig nyckeln själv. | Adminnyckeln som appen skickar i admin-anrop, jämförs i konstant tid. Saknas den svarar alla admin-anrop (även `setup`) `E_SETUP` "Adminnyckel saknas i Script Properties". Rotation ("Ny nyckel" i appen): klistra in den nya nyckeln här och kör Anslut-guiden om. |
| `CONFIG_FILE_ID` | `setup` (Anslut-guiden) – manuellt bara som reserv | Drive-id för `telexia-bokning-config.json` (skrivs av appen, läses av scriptet, cache 10 min). |
| `INBOX_FILE_ID` | `setup` (Anslut-guiden) – manuellt bara som reserv | Drive-id för `telexia-bokning-inbox.json` (skrivs bara av scriptet, under lås). |
| `CACHE_FILE_ID` | `setup` (Anslut-guiden) – manuellt bara som reserv | Drive-id för `telexia-bokning-cache.json` (geokodning inkl. `omrade` sedan steg 2c, restid, ICS-reserv). |
| `MAPS_API_KEY` | CJ (valfri) | Google Maps-nyckel. **API-begränsning:** bara **Geocoding API**, **Distance Matrix API** och **Places API (New)** (steg 2b – det är *Places API (New)*, inte det äldre "Places API", som ska vara aktiverat i Cloud-projektet och tillåtet på nyckeln; saknas det svarar Places 403 `PERMISSION_DENIED` → `ping.mapsVarning` "Places API ej aktiverat" och adressfälten fungerar som fritext). **Applikationsbegränsning: Ingen** – referrer-/IP-begränsning fungerar inte från Apps Script (UrlFetchApp skickar ingen referrer och Googles IP-pool går inte att vitlista; resultatet blir `REQUEST_DENIED`, `ping.mapsVarning` "Nyckeln avvisad" och schablonrestid överallt). Saknas nyckeln → schablonrestid och inga adressförslag (`adressforslag` svarar `kalla:'ingen'`, inget fel). Nyckeln skickas till Places i headern `X-Goog-Api-Key` (aldrig i URL:en) och till Geocoding/Distance Matrix som `key=`-parameter; den loggas aldrig och når aldrig klienterna. |
| `MAPS_DAILY_CAP` | CJ (valfri) | Dagstak för Maps-element, default 1000. Geokodning och varje Distance Matrix-element räknas som förut; sedan steg 2b räknas även **varje Places-anrop som 1 element** (cache-träffar räknas inte). Nått tak → schablonrestid och `adressforslag` → `kalla:'ingen'`. |
| `INBOX_REV` | scriptet (version 7) | `rev` för den inkorgskopia som senast lades i CacheService (skrivs bara under låset, av `writeInbox` eller en låst `readInbox`). En cache-träff i `readInbox` godtas bara när kopians `rev` matchar – annars läses Drive. Behöver aldrig sättas för hand; saknas den läser scriptet Drive tills nästa låsta läsning/skrivning. |
| `maps_elements_<YYYYMMDD>` | scriptet | Räknare för Maps-element per dag (gallras efter 7 dagar). |
| `book_count_<YYYYMMDD>` | scriptet | Global bokningsräknare per dag (`MAX_BOOK_GLOBAL_D`, gallras efter 7 dagar); `ping.bokningarIdag` läser dagens värde. |
| `andr_count_<YYYYMMDD>` | scriptet | Global räknare för bokarnas egna ändringar per dag (`egen-rebook`/`egen-cancel`/`egen-update`, `MAX_ANDR_GLOBAL_D`, gallras efter 7 dagar) – skyddar MailApp-kvoten. |
| `avstamning_saknas` | scriptet (`dailyMaintenance`, `purge` stryker id:n) | `{ ts, ids:[bokningId…] }` – avstämningens lista "saknas i inkorgen" (4.11). Giltig 36 h efter `ts` (CacheService klarar högst 6 h, därför Script Property – A48); högst 100 id:n. |
| `maintenance_senast` | scriptet | ISO-tid för senaste **lyckade** `dailyMaintenance` (`ping.underhallSenast`; sanity check V1 – triggern kör fortfarande). |
| `BLOCK_KALENDER_ID` | scriptet (version 11, `syncBlock_`) | Id för Google-kalendern **"Pipeline – restid"** som scriptet skapar (`Calendar.Calendars.insert`) första gången blocksynken körs med `installningar.blockSynkAktiv = true`. Kalendern läses aldrig av modulen. Är kalendern borttagen (404) skapas en ny och propertyn skrivs om; är synken av och kalendern borta rensas propertyn. Ta aldrig bort propertyn för hand medan kalendern finns – då skapas en till kalender och den gamla blir kvar med sina block. |
| `KONTON_FILE_ID` | `setup` (Anslut-guiden/Återanslut, version 13 – fältet `kontonFileId` är valfritt i anropet) | Drive-id för `telexia-bokning-konton.json` (bokarkonton; skrivs bara av scriptet under lås). Saknas den svarar alla `konto-*`-endpoints `E_SETUP` "Kontofilen saknas – kör Återanslut i appen" och `ping.konton.kontonFil` är `false`; övriga endpoints påverkas inte. |
| `KONTON_REV` | scriptet (version 13) | Som `INBOX_REV`, för kontofilens CacheService-kopia. |
| `SESSION_SECRET` | scriptet (`install()`/`setup`, version 13) | 32 slumpbyte (base64url) – HMAC-nyckel för bokarnas sessions-tokens. **Rotation (radera propertyn och kör `install()`) loggar ut alla bokare** – medveten nödbroms vid misstänkt läckage. Visas aldrig i något svar. |
| `LOSEN_PEPPER` | scriptet (`install()`/`setup`, version 13) | 32 slumpbyte (base64url) – server-hemlighet i lösenordshashen. **Rotera ALDRIG när konton finns**: alla lösenord blir ogiltiga och varje bokare måste köra "Glömt lösenord". Ta aldrig bort propertyn för hand. |
| `login_fail_<YYYYMMDD>` | scriptet (version 13) | Misslyckade inloggningar per dag (globalt, `KONTO_MAX_LOGIN_FAIL_GLOBAL_D` 300 → `E_RATE` på alla inloggningar; `ping.konton.inloggningsforsokIdag`; gallras efter 7 dagar). |
| `konto_mail_<YYYYMMDD>` | scriptet (version 13) | Skickade konto-mail per dag (verifiering/återställning/"konto finns", `KONTO_MAX_MAIL_D` 30; `ping.konton.kontoMailIdag`; gallras efter 7 dagar). Notiserna till CJ räknas inte här. |
| `BLOCK_SYNK_SENAST` | scriptet (version 11) | `{ ts, in, bort, andrade, kvar, antal, fel, hoppad }` från senaste blocksynken som rörde kalendern (`ping.blockSynk.senast`). `kvar` > 0 = skrivtaket/tidsbudgeten nåddes, nästa körning fortsätter; `hoppad` = `'inkorg'`/`'outlook'` när körningen hoppades över för att källan var ofullständig (annars `''`). |

Saknas något av de tre fil-id:na, eller misslyckas filkontrollen (filen i papperskorgen, fel namn, fel filtyp, annan ägare), svarar alla anrop som behöver filerna `E_SETUP`. `ping` svarar alltid: `konfigurerad:false` med `orsak` (t.ex. `"cache: Filen ligger i papperskorgen"`, `"Fil-id saknas i Script Properties: config, inbox, cache"`, `"Adminnyckel saknas i Script Properties"`) – `ping` kontrollerar alla tre filerna (namn, filtyp, ägare, papperskorg), inte bara att id:na finns; kontrollen är cachad 10 min per fil (ping är oautentiserat och får inte kunna driva Drive-kvoten), färsk kontroll görs i `setup`.

## Deploy (första gången)

1. script.google.com → Nytt projekt, döp det till "Pipeline bokning".
2. Projektinställningar: tidszon Europe/Stockholm, kryssa i "Visa manifestfilen appsscript.json i redigeraren".
3. Klistra in `appsscript.json`, `Code.gs`, `Calendar.gs`, `Availability.gs` och `Konton.gs` (skapa filerna med + › Skript).
4. Tjänster (+) › Google Calendar API › v3 (identifierare `Calendar`) – manifestet innehåller redan posten, men kontrollera att den syns.
5. Kör `install()` en gång i redigeraren (skapar triggarna `dailyMaintenance` och `refreshIcsCache` samt, sedan version 13, hemligheterna `SESSION_SECRET`/`LOSEN_PEPPER` i Script Properties). Förväntat: "Google har inte verifierat den här appen" → Avancerat → Fortsätt. Godkänn Kalender, Drive, externa anrop, e-post och triggers. Byt **inte** till ett eget GCP-projekt.
6. Distribuera › Ny distribution › Webbapp: "Kör som: Jag", "Vem har åtkomst: **Alla**" (inte "Alla med Google-konto"). Kopiera URL:en som slutar på `/exec`.
7. Kontroll: öppna `<URL>?action=ping` i ett inkognitofönster – JSON ska visas (`ok:true`, `scriptVersion:13`), inte en inloggningssida.
8. Ge URL:en till Claude → `BOK_SCRIPT_URL` sätts i `index.html` och i sajtens `assets/js/bokning.js` (samma värde).
9. **Anslut brevlådan från appen** (Bokningar → Inställningar → Anslut brevlåda, spec 10.1 steg 4): appen kör `ping`, skapar de tre Drive-filerna i "Min enhet", genererar adminnyckeln och visar den – **klistra in den som `ADMIN_KEY` i Projektinställningar → Script Properties** och klicka "Fortsätt". Appen anropar då `setup` med fil-id:na: `setup` kräver att `ADMIN_KEY` finns och stämmer (annars `E_SETUP`/`E_ADMIN`), verifierar filerna (namn `telexia-bokning-*.json`, JSON, ägare = kontot scriptet körs som, inte i papperskorgen), sparar `CONFIG_FILE_ID`/`INBOX_FILE_ID`/`CACHE_FILE_ID`, seedar tomma filer, skapar triggarna `dailyMaintenance` och `refreshIcsCache` idempotent (`install()`) och svarar med `ownerEmail` + kalenderlistan. Det enda som klistras in för hand är `ADMIN_KEY` (och `MAPS_API_KEY` om Maps ska användas); fil-id:na sätter `setup`. Kontroll: `<URL>?action=ping` → `konfigurerad:true`, `orsak:""`.

### Verifiera i redigeraren efter inklistring (en gång per deploy)

- `Logger.log(Session.getEffectiveUser().getEmail())` → CJ:s adress, **inte tom sträng**. Tom sträng = scopet `userinfo.email` saknas/är inte godkänt; då svarar all filhantering `E_SETUP` "Scriptet saknar behörighet userinfo.email".
- `Logger.log(Session.getScriptTimeZone())` → `Europe/Stockholm` eller en zon med samma regler (Googles meny ger ofta `Europe/Berlin`; godtas av `tidszonOk()`). En zon med andra regler ger `E_INTERNAL` på alla anrop.
- `Logger.log('Storgatan 1, Sverige!'.replace(/[^\p{L}\p{N}\s]/gu, ' '))` → `Storgatan 1  Sverige ` utan `SyntaxError` (Unicode property escapes i `normalizeAdressKey`, Availability.gs). Vid fel: byt regexen till `/[^a-z0-9åäöéü\s]/g` (strängen är redan i gemener).
- `runAvailabilityTests()` → "Alla N test OK" i loggen (version 10: 88 test).
- `runKontoTests()` (version 13) → "Alla N test OK" + **hashtiden** i loggen: `Hashtid (400 iterationer): NNN ms – mål ≤ 300 ms`. Uppmätt 2026-09-19: 2000 iterationer tog 1459 ms i Apps Script (~0,7 ms per HMAC), därför `KONTO_LOSEN_ITER = 400` (~290 ms). Ligger den klart över 300 ms sänks konstanten ytterligare; ligger den långt under kan den höjas. Värdet lagras i varje hash-sträng, så ändringen bryter inga befintliga lösenord (de hashas om vid nästa lyckade inloggning).
- Med Maps-nyckel (steg 2b): kör `debugPlaces('Storgatan 1')` i redigeraren → loggraden `{"status":"ok","antal":N,"nyckel":true,"varning":""}` med `antal > 0`. `status:"nekad"` = Places API (New) är inte aktiverat i Cloud-projektet eller inte tillåtet på nyckeln (403) – aktivera och kör igen. Funktionen loggar aldrig nyckeln eller förslagstexterna; den räknar 1 element mot `MAPS_DAILY_CAP`.
- `<URL>?action=ping` → `konfigurerad:true` och `orsak:""` när Anslut-guiden körts (fil-id:n + `ADMIN_KEY` finns och alla tre filerna klarar kontrollen); `mapsVarning` tom när Maps-nyckeln är rätt begränsad.
- Med ICS: sätt `outlookIcsUrl` tillfälligt till en långsam https-endpoint, kör två `reserve` parallellt från konsolen – ingen ska ge `E_LOCK` (ICS hämtas före låset); loggradens `ms` för `reserve` ska ligga under ~5 s när ICS är cachad.
- Kör `refreshIcsCache()` en gång för hand i redigeraren (med `outlookIcsUrl` satt) → loggraden `{"trigger":"refreshIcsCache","ok":true,"ms":…,"kalla":"live"}`; en andra körning direkt efter → `"kalla":"hoppad"` (cachen är färsk). Kontrollera under Triggers att båda triggarna finns (`dailyMaintenance` dagligen 03–04, `refreshIcsCache` var 10:e minut).
- Kör `dailyMaintenance()` en gång för hand i redigeraren → loggraden `{"trigger":"dailyMaintenance","ok":true,"ms":…,"raknare":…,"inkorg":…,"utanImport":…,"geokod":…,"restid":…,"omrade":…,"saknas":…,"fel":[]}` (Körningar) och `ping.underhallSenast` satt. Med CJ:s riktiga inkorg ska `ms` ligga långt under 60 000 (gränsen är 6 min).
- Med CJ:s stora Outlook-flöde: kör `calendar-preview` två gånger inom 15 min från appen (Kalenderkoll) – andra gången ska `icsStatus.cachad` vara `true` och loggradens `ms` betydligt lägre (ingen UrlFetch, ingen parsning; se "ICS-cache"). `icsStatus.hamtningMs` visar hur lång tid Outlook tog; > 5 000 → `langsam:true` och reservkopian används vid nästa cache-miss (flaggan gäller 30 min, sedan `false` även om `ics:meta` ligger kvar). Kör därefter `availability` (bokningssidan, mötestyp med restid) två gånger med > 6 h mellanrum eller efter `CacheService`-utgång: loggradens `ms` ska ligga < 3 000 exkl. Outlook-hämtning – cache-filen läses högst en gång per anrop (memo, A51), inte en gång per ICS-plats.
- **V13 – anonymisering (purge på PASSERAD testbokning):** kör `purge` med bokningens id från appen (radera testprocessen) eller från konsolen, hämta sedan händelsen i redigeraren: `Logger.log(JSON.stringify(Calendar.Events.get('<bokningskalenderns id>', '<eventId>')))` → `summary` = `Möte (borttaget)`, `location`/`description` tomma och `extendedProperties.private` **utan** `bokningId`/`bokareId`/`pipelineId`/`motestypId`. Ligger nycklarna kvar (advanced service-bryggan har då tappat `null`-fälten) → byt i `purgeAnonymiseraHandelse` till tomma strängar (`bokningId: ''` …) eller patcha via `UrlFetchApp` mot REST-API:t med `ScriptApp.getOAuthToken()`. Avstämningen och purge-sökningen hoppar oavsett över händelser med titeln `Möte (borttaget)`, så ett kvarlämnat bokningId ger varken 'saknas i inkorgen' eller en ny patch.
- **Avstämning live:** skapa en händelse med `extendedProperties.private.bokningId` (36 tecken) i Bokningar-kalendern utan inkorgspost → "Kontrollera avstämning" i appen ska visa 'saknas i inkorgen' **direkt**, före nattkörningen (A52).
- **Blocksynk (version 11):** kryssa i "Synka restid och marginal till Google Kalender" (Inställningar › Kalendrar), spara, klicka "Synka nu" → kalendern **Pipeline – restid** dyker upp i Google Kalender (under "Mina kalendrar") med röda restidsblock och grå marginalblock; Script Properties får `BLOCK_KALENDER_ID` + `BLOCK_SYNK_SENAST`; Körningar visar `{"trigger":"syncBlock","ok":true,"ms":…,"in":N,"bort":0,"andrade":0,"kvar":0,"antal":N,"fel":0}`. Kör `refreshIcsCache()` för hand → två loggrader (`refreshIcsCache` + `syncBlock` med `in:0` när inget ändrats). Kryssa ur, "Synka nu" → blocken i fönstret försvinner (`bort:N`), kalendern och passerade block ligger kvar. Kontroll av K6: Kalenderkoll (`calendar-preview`) ska **inte** visa några block, och `calendars-list` ska ge `blockKalender:true` på kalendern.

## Uppdatera scriptet (varje senare ändring)

Klistra in ny kod → Distribuera › **Hantera distributioner** → pennan → Version: **Ny version** → Distribuera.
**Aldrig "Ny distribution"** – det ger en ny URL och `BOK_SCRIPT_URL` skulle behöva ändras i både appen och sajten.
Höjs `SCRIPT_VERSION` i `Code.gs` ska `MIN_SCRIPT_VERSION` höjas i samma ordning: script (ny version) → sajten → appen.
**M5 = SCRIPT_VERSION 3** (purge på riktigt, `dailyMaintenance`, nya ping-fält): deployen är en **Ny version av den befintliga distributionen** (A44). `MIN_SCRIPT_VERSION` lämnas på 1 i appen och sajten (inget bryts för bokare), men appens Drift-panel varnar "Scriptversion 2 – purge saknas" (`PURGE_SCRIPT_VERSION = 3` i `index.html`) tills den nya versionen är distribuerad – radering av processer/kunder med bokningar avbryts mot ett script utan purge (`E_NOT_IMPLEMENTED`).
**Steg 2a = SCRIPT_VERSION 4** (bokarens självservice: `egen-rebook`/`egen-cancel`/`egen-update`, `hello.egna` med `kanAndras`): också en **Ny version av den befintliga distributionen**. `MIN_SCRIPT_VERSION` höjs till 4 i **både** `index.html` och `bokning.js` (sidans Omboka/Ändra/Avboka-knappar och appens spegling av historik `av:'bokare'` förutsätter de nya endpoints – ett äldre script svarar `E_VALIDATION` "Okänd åtgärd"). Ordning: script (Ny version) → sajten → appen.
**Steg 2b = SCRIPT_VERSION 5** (adressförslag: endpoint `adressforslag` via Places API (New), `placeId` bredvid adress i `availability`/`reserve`/`book`/`geocode`/`egen-update`/`egen-rebook`/`rebook`): **Ny version av den befintliga distributionen**. `MIN_SCRIPT_VERSION` **förblir 4** i appen och sajten – autocomplete är valfri: mot ett äldre script svarar `adressforslag` `E_VALIDATION` "Okänd åtgärd" och saknas Maps-nyckel/Places API svarar det `kalla:'ingen'`; i båda fallen fungerar adressfälten som fritext utan felruta. Utöver scriptet krävs **Places API (New)** aktiverat i Cloud-projektet och tillåtet på nyckeln (se `MAPS_API_KEY`).

**Steg 2c = SCRIPT_VERSION 6** (områdesetikett för bokare + restidsvisualisering i Kalenderkoll, CJ:s beslut 2026-09-16): **Ny version av den befintliga distributionen**. `MIN_SCRIPT_VERSION` **förblir 4** i appen och sajten – de nya fälten är valfria: `availability`-block får `omrade` (områdesetikett), `calendar-preview` får `omrade` per händelse och `resor` per dag; saknas fälten (äldre script) renderar klienterna som förut. Se "Områdesetikett och restid i Kalenderkoll" nedan.

**Version 7 = SCRIPT_VERSION 7** (optimering + restid tydlig + Teams-fix, CJ:s beslut 2026-09-16): **Ny version av den befintliga distributionen**. `MIN_SCRIPT_VERSION` **förblir 4** i appen och sajten – alla nya fält är valfria (`availability`-block av typ `paus`; `calendar-preview.resor` utelämnar 0-ben; mejltexten). **Driftinstruktion: kör `install()` en gång i redigeraren efter inklistringen** så att den nya triggern `refreshIcsCache` skapas (install tar bort och återskapar båda triggarna idempotent; Anslut-guidens `setup` gör detsamma). Se "Version 7" nedan.

**Version 9 = SCRIPT_VERSION 9** (manuell restidsklassning och rättad adress per kalenderhändelse, CJ:s beslut 2026-09-16): **Ny version av den befintliga distributionen**. `MIN_SCRIPT_VERSION` **förblir 4** i appen och sajten – allt nytt är valfria fält (`KALENDER_IGNORERA`-postens `online`/`adress`/`lat`/`lng`/`omrade` och `lage:'restid'`, `calendar-preview`-postens `overrideOnline`/`overrideAdress`/`serieId`, `geocode`-svarets `omrade`); ett äldre script ignorerar de nya fälten (en post med `lage:'restid'` gör då varken ignorera eller räkna), en äldre app skickar dem inte. Ingen `install()` behövs. Se "Version 9" nedan.

**Version 10 = SCRIPT_VERSION 10** (ren restid, marginal efter varje möte, restid delad runt platslösa möten – CJ:s beslut 2026-09-17): **Ny version av den befintliga distributionen**. `MIN_SCRIPT_VERSION` **förblir 4** i appen och sajten – allt nytt är valfria fält (`availability`: `slot.restid.inDelar`/`utDelar` och `data.pausMin`; `calendar-preview`: `resor[].delar`; `installningar.marginalFysisktMin`/`marginalOnlineMin` med defaults 15/5), `inBlock`/`utBlock`/`resor[].start`/`slut` behåller sin form som **yttre spann** så att äldre klienter ritar som förut. Restiden i alla svar är nu Googles råa körtid (inget påslag) – siffrorna blir därmed lägre än i version ≤ 9, och bufferten efter varje möte syns i stället som paus-block/`cooldownMin`. Ingen `install()` behövs (inga nya triggers/scopes); `?action=ping` → `scriptVersion: 10`. Se "Version 10" nedan.

**Version 11 = SCRIPT_VERSION 11** (blocksynk till Google Kalender – A61, CJ:s beslut 2026-09-17 "Samtliga block ska synkas med Google-kalendern"): **Ny version av den befintliga distributionen**. `MIN_SCRIPT_VERSION` **förblir 4** i appen och sajten – allt nytt är valfritt (`installningar.blockSynkAktiv` med default `false`, admin-endpointen `block-sync`, `ping.blockSynk`, `calendars-list`-postens `blockKalender`); ett äldre script ignorerar fältet och svarar `E_VALIDATION` "Okänd åtgärd" på `block-sync` (appens knapp visar felet). **Ingen `install()` behövs** – blocksynken kör i den befintliga triggern `refreshIcsCache` och manifestet är oförändrat (`Calendar.Calendars.insert` täcks av scopet `auth/calendar`), så ingen ny auktorisering krävs. `?action=ping` → `scriptVersion: 11`. Se "Version 11" nedan.

**Version 13 = SCRIPT_VERSION 13** (bokarkonton – steg 3, CJ:s beslut 2026-09-19): **Ny version av den befintliga distributionen**, men **kontraktet ändras**: `MIN_SCRIPT_VERSION` höjs till **13 i både `index.html` och `bokning.js`**. Bokarkoden `k` accepteras bara för CJ-bokare (`arCj`) – alla gamla `?k=`-länkar för vanliga bokare svarar `E_KEY` från och med distributionen (inga befintliga bokare sparas, B12). **Driftinstruktion:** (1) klistra in alla fyra `.gs`-filer (ny fil `Konton.gs`), (2) kör `install()` en gång i redigeraren – skapar `SESSION_SECRET` och `LOSEN_PEPPER` (triggarna återskapas som förut), (3) kör `runKontoTests()` och läs av hashtiden, (4) Ny version, (5) i appen: Inställningar › **Återanslut** (skapar `telexia-bokning-konton.json` och kör `setup` med fyra id:n → `ping.konton.kontonFil:true`), (6) lägg upp tillåtelselistan under Bokare, (7) deploya sajten. Inga nya scopes (MailApp/Drive/Properties fanns redan). Se "Version 13" nedan.

## Version 13 – bokarkonton: e-post + lösenord, tillåtelselista, kontofil (SCRIPT_VERSION 13)

Spec: `02 Projects/Pipeline/[C] Bokningsmodul - specifikation steg 3 bokarkonton.md` (avsnitt 4). Koden ligger i `Konton.gs`; `Code.gs` ändrades i `authBokare`, `normalizeConfig` (`domaner`), `configVarningar`, `handleSetup`/`setupFileIds` (fjärde fil-id, valfritt), `handleHello` (`helloBokareExport_`), `handleBook` (`bokning.bokare`-snapshot), `bookingEventResource` ("Bokad av: namn, e-post, mobil"), `rebookBokare` + bokarmejlen (`bokareForPost_` – kontot som reserv när appen inte importerat bokaren än), `handlePing` (`konton`), `HANDLERS`, `install()`, `dailyMaintenance` (`gallraKonton_`), `gallraRaknare`.

- **Kontofilen** `telexia-bokning-konton.json` (skapas av appens Återanslut, id via `setup.kontonFileId` → `KONTON_FILE_ID`): `{ schemaVersion, rev, updatedAt, updatedBy:'script', konton:[{ id:'bokare_<uuid>', epost, doman, listaId, fornamn, efternamn, mobil, pipelineId, status:'overifierad'|'aktiv', losenord:{ hash:'v1$<iter>$<salt>$<hex>', version }, token:{ typ:'verifiera'|'aterstall', hash, utgar }|null, misslyckade, sparradTill, mail:{ datum, antal }, skapadTs, verifieradTs, senastInloggadTs, historik:[…≤50] }] }`. Skrivs bara av scriptet under `withScriptLock`; CacheService-kopia `konton:<fileId>` med rev-kontroll mot `KONTON_REV` (samma mönster som inkorgen). Appen läser filen **aldrig** direkt – bara `konton-list`, som utelämnar `losenord`/`token`/`misslyckade`/`historik`.
- **Tillåtelselistan** kommer från appen i config-filen som `domaner:[{ id, doman, pipelineId, aktiv }]` där `doman` är en domän (`byra.se`) eller en hel adress (`kalle@gmail.com`). `matchaLista_(epost, lista)`: aktiv post med exakt adressträff först, annars aktiv post med exakt domänträff (allt efter `@`, gemener; subdomäner räknas inte). `config-push` varnar för poster utan giltig pipeline, ogiltigt format och tom lista.
- **Effektiv bokare** (`effectiveBokare_`): id, namn (förnamn + efternamn), e-post, mobil, `organisation` = domänen, `harKonto:true`; `pipelineId`/`tillatnaMotestypIds`/`aktiv` från appens `config.bokare`-post med samma id när den finns (efter import), annars kontots pipeline (från listposten vid registreringen) och `aktiv` = matchar en aktiv listpost. Konsekvens: CJ:s Aktiv-kryssruta stänger av vid nästa anrop; en borttagen listpost stänger oimporterade konton direkt och importerade via appens `aktiv=false`.
- **Autentisering:** alla bokar-endpoints (`hello`, `availability`, `reserve`, `release`, `book`, `geocode`, `adressforslag`, `egen-*`, `konto-byt-losenord`, `konto-profil`) tar `s` (session) **eller** `k` (bara CJ-bokare). Sessionen är `base64url(JSON{ v:1, id, pv, exp, iat }).base64url(HMAC-SHA256(payload, SESSION_SECRET))`, statslös, 30 dagar (`komIhag`) eller 12 h, och bunden till `losenord.version` (`pv`) – lösenordsbyte/återställning loggar ut alla andra enheter. Fel: `E_SESSION` (format/signatur/utgången/versionen stämmer inte/kontot inte aktivt), `E_INAKTIV` (avstängd). Räknarnyckeln per bokare blir `id_<kontoId>` (ingen kodHash) – alla handlingsgränser gäller oförändrat.
- **Lösenord:** 6–128 tecken (bara längd), får inte vara lika med e-posten. Hash `v1$<iter>$<saltB64url>$<hex>` = HMAC-SHA256(lösenord, salt) itererat `iter−1` gånger med `LOSEN_PEPPER` som nyckel; `KONTO_LOSEN_ITER` 400 (uppmätt 2026-09-19: 2000 iterationer = 1459 ms i Apps Script, 400 ≈ 290 ms – justeras efter `runKontoTests()`-mätningen; en hash med annat iter verifieras ändå och hashas om vid nästa inloggning). Jämförelse i konstant tid. Klartext bara i request-body, loggas aldrig.
- **Oautentiserade endpoints** (alla under den globala gränsen `rl:konto` 60/10 min, e-postbundna dessutom `rl:login:<hash>` 20/10 min):
  - `konto-registrera { epost, fornamn, efternamn, mobil, losenord }` → `{}`. Ingen listträff → `E_DOMAN`. Finns kontot: aktivt → mail "du har redan ett konto" (+ återställningslänk 1 h); overifierat → profil och lösenord skrivs över med den nya inskickningen + nytt verifieringsmail. Nytt konto → `status:'overifierad'`, verifieringsmail (länk `bokningSidaUrl?verifiera=<token>`, 24 h). Global mailgräns nådd → `E_RATE` typ `mail`.
  - `konto-verifiera { token }` → `{ session, exp, bokare }` (12 h, kontot `aktiv`, notis till CJ "Ny bokare: …"). Okänd/utgången/använd länk → `E_TOKEN`.
  - `konto-logga-in { epost, losenord, komIhag }` → `{ session, exp }`. Okänd adress eller fel lösenord → `E_LOGIN` (identiskt). Rätt lösenord men overifierat → `E_OVERIFIERAD` + nytt verifieringsmail (max 3/dygn per konto). Avstängd → `E_INAKTIV`. 8 fel → `sparradTill` 15 min → `E_RATE` typ `inloggning`; 300 fel/dygn globalt → `E_RATE` för alla. Kontot skrivs bara när något ändrats (räknare, dygnsstämpel `senastInloggadTs`, omhashning).
  - `konto-glomt { epost }` → `{}` alltid. Aktivt konto → återställningsmail (`?aterstall=<token>`, 1 h); overifierat → nytt verifieringsmail; okänt → inget.
  - `konto-aterstall { token, losenord }` → `{ session, exp }` (12 h); `losenord.version++`. Utgången/okänd → `E_TOKEN`.
- **Med session:** `konto-byt-losenord { s, gammalt, nytt, komIhag? }` → ny session (övriga enheter loggas ut; fel gammalt → `E_LOGIN`); `konto-profil { s, fornamn, efternamn, mobil }` → `{}` (e-post kan inte bytas).
- **Admin:** `konton-list { adminKey }` → `{ konton:[{ id, epost, doman, listaId, fornamn, efternamn, mobil, pipelineId, status, skapadTs, verifieradTs, senastInloggadTs }], rev }`; `konto-radera { adminKey, id }` → `{ borttaget }` (tar bort kontot, aldrig inkorgsposter). `setup` tar `kontonFileId` (valfritt) och svarar `kontonFil`. `ping.konton` = `{ kontonFil, kontonAntal, kontoMailIdag, inloggningsforsokIdag, hemligheter }`.
- **Bokningen (B9):** inkorgsposten får `bokare:{ namn, epost, mobil }` (snapshot vid `book`; CJ-bokare `{ namn:'CJ', epost:'', mobil:'' }`); kalenderbeskrivningen börjar "Bokad av: Anna Ek, anna@byra.se, 070-…" – alltid, oberoende av `kontaktuppgifterIKalender` (som bara styr kundens kontakt). Notismejlet till CJ om ny bokning är oförändrat. Appen speglar snapshoten till händelsens `bokadAvKontakt` (K2).
- **Mail (plain text, avsändarnamn "Pipeline bokning"):** verifiering, "konto finns redan", återställning, samt notis till `notisEpost` när ett konto verifierats (namn, e-post, mobil, listpost, pipeline). Gränser: 30 konto-mail/dygn globalt (`konto_mail_<YYYYMMDD>`), 3/dygn per konto (`konto.mail`); mailfel loggas som `historik` `mailfel` på kontot och fäller aldrig anropet.
- **Gallring** (`dailyMaintenance`, eget lås efter inkorgssteget): overifierade konton äldre än 7 dagar raderas, utgångna tokens nollas, historik klipps; loggradens `konton`/`tokens`. Aktiva konton raderas aldrig automatiskt – CJ tar bort i appen (`konto-radera`).
- **Säkerhet:** identiska svar på `konto-registrera`/`konto-glomt` oavsett om kontot finns (bara `E_DOMAN` avslöjar något, avsiktligt); tokens lagras bara som SHA-256; sessioner och lösenord når aldrig loggen (`bokareId` i loggraden är kontots id); `E_LOGIN`-texten är statisk; alla fält från kontot renderas av klienterna via `escapeHtml`/`textContent`.

## Version 12 – prestanda: properties-memo, batchade cache-läsningar, förberäknad kalender i triggern, tidsmätning (SCRIPT_VERSION 12)

CJ 2026-09-17: *"kalendern är väldigt långsam att ladda i Pipeline, och bokningsflödet är segt."* Mätningen samma dag: `ping` tog 3–5 s live fast den inte gör något tungt – Apps Scripts fasta overhead är 1,2–2,5 s och resten var **många små tjänsteanrop i serie**: `PropertiesService.getProperty` per nyckel (ping ≈ 15), `CacheService.get` per nyckel (calendar-preview: ett per restidspar och ett per platstext – hundratals för 8 veckor), `Calendar.Events.list` live varje gång (busy:<datum>-cachen på 60 s hann gå ut mellan anropen) och inkorgen från Drive när ingen låst operation nyligen fyllt cachen. Målet: `calendar-preview` ≈ overhead + cache (~2 s), `availability` ≈ overhead + varma cacher. Inga kontraktsändringar – alla nya fält är valfria (`MIN_SCRIPT_VERSION` förblir 4).

**K1 – Script Properties-memo per körning (Code.gs `getProp`/`setProp`/`deleteProp`):** `getProp` läser ur ett memo (`PROP_MEMO_`) som fylls **en** gång per körning med `getProperties()` (lat, vid första `getProp`); `setProp`/`deleteProp` skriver igenom och uppdaterar memot; memot nollställs per körning (`propResetMemo_` i `doPostInner_`, `refreshIcsCache`, `dailyMaintenanceInner_` – bredvid övriga `*ResetMemo_`). Ny `getPropFarsk(name)` läser alltid direkt (och uppdaterar memot) och används där läs-ändra-skriv mot en räknare sker – `addMapsElements`, `countBooking`, `countAndring` (`maps_elements_`/`book_count_`/`andr_count_`) – samt i `readInbox`:s `INBOX_REV`-kontroll (koherens med låshållarens skrivning) och i `avstamningStryk` (`avstamningSaknas({ farsk:true })` – purge/cancel stryker id:n ur `avstamning_saknas` under låset, och memot fylldes redan vid `authAdmin` före låset). Allt annat (fil-id:n, `ADMIN_KEY`, `MAPS_API_KEY`, `MAPS_DAILY_CAP`, avstämningens läsning i `calendar-preview`/`ping`, `BLOCK_*`, `maintenance_senast`) via memot. `gallraRaknare` raderar via `deleteProp` så att memot hålls koherent.

**K2 – Batchade CacheService-läsningar (Availability.gs):** (a) `travelSecondsForPairs_` samlar alla `restid:<nyckel>` och läser dem med **en** `getAll` (`cacheGetJsonAlla_`) i stället för `cacheGetJson` per par; (b) ny `geoPrimeMemoBatch_(texter)` sha256-nycklar alla unika texter, gör **en** `getAll` på `geo:<hash>` och seedar träffarna i `AVAIL_GEO_MEMO` (samma form som `geocodeAddress` cachar) – anropas i `geokodaAnkare` (unika platstexter, `ankarTexter_`) före per-post-anropen och i `computeAvailabilityCore` för bokarens adress + ankartexter. Nycklar som redan slagits upp i körningen (`AVAIL_GEO_PRIMAT_`, även missar) hoppas över, så `computeAvailabilityCore` + `geokodaAnkare` kostar ett `getAll` för samma texter, inte två. Semantiken är oförändrad: miss → cache-filen/Maps som förut; `cachePutJson` per post finns kvar (bara vid miss); placeId-flödet berörs inte.

**K3 – Förberäkning i triggern (`refreshIcsCache`, var 10:e minut, EFTER ICS-läsningen och FÖRE `syncBlock_`; `previewSnapKor_`):** (a) **inkorgscache:** `LockService.getScriptLock().tryLock(0)` → `readInbox()` under låset (fyller CacheService, `LAS_HALLS_`) → `releaseLock`; är låset upptaget läses inkorgen utan fyllning (`las:false` i loggraden); (b) **busy-värme:** fönstret W = `[idag−7, horisontTom+7]` (samma som appens `bokKalFonster`) läses med `buildBusyList(from, to, { config, inbox, farsk:true, cacheTtlS: BUSY_CACHE_VARM_S })` – `readBusy` har fått `opts.cacheTtlS` (default `KAL_BUSY_CACHE_S` = 60) så triggern skriver `busy:<datum>` med **11 min** TTL (nästa körning skriver om innan de går ut; klienternas egna läsningar skriver 60 s som förut); (c) **snapshot:** förhandsgranskningen räknas med samma kärna som `calendar-preview` – utbruten till `previewBerakna_(config, inbox, from, to, opts)` → `{ handelser, resor, varningar, icsStatus, obesvarade, restidOverCap, geoOverCap, busy }` – och sparas som `cacheChunkedPut_('preview:snap', { from, to, genererad, handelser, resor, varningar, icsStatus, obesvarade, restidOverCap, geoOverCap }, PREVIEW_SNAP_S = 660)` (gzip i bitar, samma format som inkorgscachen; aldrig BusyItems); (d) **blocksynken återanvänder beräkningen:** `syncBlockBerakna_(config, inbox, forberaknat)` filtrerar triggerns `busy` till `[idag, horisontTom]` och `kalla !== 'reservation'` och räknar om `resor` ur det filtrerade (redan geokodade) `busy` med `previewResor` – restidsparen ligger i CacheService efter triggerns beräkning (ett `getAll`, inga nya Maps-anrop i normalfallet), så kedjan A→C runt en aktiv reservation B blir densamma som utan reservationer (som före version 12); ben som ändå rör en reservation utelämnas (skyddsnät). Ingen ny kalenderläsning/geokodning; utan `forberaknat` (`block-sync`-endpointen) räknar den som förut. (e) **generationskontroll:** `previewSnapKor_` läser stämpeln `preview:gen` före beräkningen och jämför efter – har en invalidering (`previewSnapRensa_`, K5) skett under tiden skrivs ingen snapshot, de nyss skrivna `busy:<datum>` (11 min) tas bort igen (`clearBusyCacheWindow`) och blocksynken räknar själv; loggraden får `ok:false, skal:'invaliderad'`. **Tidsbudget: ≤ 30 s per körning i snitt** – 144 körningar/dygn mot trigger-kvoten 90 min/dygn (`syncBlock_`:s `BLOCK_SYNK_MAX_MS_TRIGGER` räknas in); överskrids den återkommande stänger Apps Script av alla triggers resten av dygnet (ingen ICS-värmning, ingen snapshot, ingen `dailyMaintenance`). Loggrad `{ trigger:'previewSnap', ok, ms, dagar, handelser, resor, las }` – aldrig titlar.

**K4 – `calendar-preview` svarar ur snapshoten:** om `req.farsk !== true` och en snapshot finns (`cacheChunkedGet_('preview:snap')`) som täcker `[from, to]` och är yngre än `PREVIEW_SNAP_S` → svaret byggs ur snapshoten filtrerad på datum (`handelser`, `resor`) med de vanliga toppnivåfälten + `cachad:true`, `snapshotTs` = snapshotens `genererad`; avstämningsvarningar ("saknas i inkorgen"), avbokade/avvisade-kvar-varningar och Maps-varningen räknas **live** mot snapshotens händelser (`previewSvar_`/`previewVarningarPost_` – Script Property ur memot, inkorgen ur CacheService, en cache-get). Annars live: ingen/utgången/för smal snapshot → `previewBerakna_` med busy-cachen som förut; `farsk:true` ("Uppdatera" i appen) → `previewBerakna_` med **färsk kalenderläsning** (`buildBusyList farsk`, busy:<datum> skrivs om med 11 min så att triggerns värme inte kortas). Täcker ett live-anrop hela W skrivs en ny snapshot – bara om `preview:gen` är oförändrad sedan beräkningen började (annars ingen snapshot; vid `farsk:true` tas även de nyss skrivna `busy:<datum>` bort). Svaret får `tider: { total, auth, inbox, busy, geo, resor, export, kalla:'snapshot'|'live' }` (heltal ms) och loggraden får `tider` (med `kalla`) via `ctx.logg`.

**K5 – Invalidering (`previewSnapRensa_` = `cacheChunkedRemove_('preview:snap')` + generationsstämpeln `preview:gen` = `String(Date.now())`, 6 h):** i `clearBusyCacheFor` och `clearBusyCacheWindow` (cancel/reject/egen-*; `rebook` och `purge` anropar dem med `{ utanSnap:true }` och invaliderar **en** gång per handler efteråt – inte en `removeAll` per bokning under låset), efter lyckad `book`, i `handleConfigPush` (inställningar/ignorera/override ändrade), i `handleSetup` och i `dailyMaintenance` när inkorgen gallrats. Stämpeln gör att en beräkning som redan pågår (triggern eller ett live-svar för hela W) inte skriver tillbaka en snapshot/`busy:<datum>` från före ändringen (`previewSnapGen_` före/efter). Efter invalidering svarar nästa `calendar-preview` live (och skriver ny snapshot bara om anropet täcker hela W) tills triggern kör igen. Reservationer invaliderar inte (5 min liv; snapshoten kan visa en reservation upp till 11 min).

**K6 – `availability`:** ingen kontraktsändring – vinner på K1/K2/K3 (`busy:<datum>` varm i 11 min → ingen `Events.list`, inkorgen varm → ingen Drive-läsning). Svaret får valfritt `tider: { total, busy, geo, restid, plan }` (heltal ms, mäts i `computeAvailabilityCore` med `deps.medTider`; `total` = hela handlern) och loggraden får `tider`. `reserve`/`book`: fresh-vägen under låset oförändrad (den färska läsningen skriver busy:<datum> med 60 s som förut).

**Konstanter (Code.gs):** `PREVIEW_SNAP_KEY = 'preview:snap'`, `PREVIEW_SNAP_S = 660`, `BUSY_CACHE_VARM_S = 660`, `PREVIEW_GEN_KEY = 'preview:gen'`, `PREVIEW_GEN_S = 21600`. Inga nya Script Properties, ingen ny auktorisering.

**Uppmätt effekt (Node-harness med räknande stubbar, fixtur: 5 platser, 31 händelser på 8 veckor, varma geo:/restid:-cacher, busy:<datum> utgången – det typiska driftläget före version 12; `v12_rakna_anrop.js`):**

| Endpoint | Före (v11) | Efter (v12) |
|---|---|---|
| `ping` | 15 × `getProperty` | 1 × `getProperties` |
| `availability` 14 d | 7 × `getProperty`, 18 × `Cache.get` + 1 `getAll`, 2 × `Events.list`, 1 Drive-läsning | 1 × `getProperties`, 8 × `get` + 3 `getAll`; **efter triggern:** 0 × `Events.list`, 0 Drive |
| `calendar-preview` 57 d live | 9 × `getProperty`, 23 × `Cache.get` + 1 `getAll`, 2 × `Events.list`, 1 Drive-läsning | 1 × `getProperties`, 8 × `get` + 3 `getAll` (per platstext/par-anropen borta) |
| `calendar-preview` ur snapshot | – | 1 × `getProperties` + 1 × `getProperty` (INBOX_REV), 6 × `get` + 2 `getAll`, **0 × `Events.list`, 0 Drive** |

I drift med fler platser/par växer "före"-kolumnen linjärt (ett `get` per platstext och per restidspar) medan "efter" är konstant. Förväntat: `calendar-preview` ≈ Apps Script-overhead + några cache-läsningar (~2 s), `availability` utan `Events.list`/Drive mellan triggerkörningarna, `ping` utan 15 seriella Properties-anrop.

**Test:** `runAvailabilityTests()` oförändrad (88 test); `v12_prestanda_test.js` (67 test: getProp-memo/getPropFarsk, batch-getAll i `travelSecondsForPairs_`, `geoPrimeMemoBatch_`, `geokodaAnkare`-priming, triggern skriver busy:<datum> 660 s + inkorgscache + preview:snap, låset upptaget, calendar-preview ur snapshot/live/farsk med identiskt innehåll, avstämning live ur snapshot, invalidering vid clearBusyCacheFor/clearBusyCacheWindow/config-push/setup, `syncBlockBerakna_` med förberäknat, availability `tider`, loggrader utan personuppgifter; granskningsfixar: invalidering mitt i triggerns/live-vägens beräkning → ingen snapshot + busy rensade + `skal:'invaliderad'`, `purge` av två bokningar → en invalidering, `avstamningStryk` läser färskt, kedjan runt en reservation räknas om i blocksynken, en miss primas inte två gånger); `v11_blocksynk_test.js` (63) och `v11_fix_review_test.js` (30) uppdaterade för memot och den nya loggraden.

Deploy: New version; **ingen `install()`** (triggers och manifest oförändrade); `?action=ping` → `scriptVersion: 12`. Ordning: script (Ny version) → appen (K7: `farsk:true` vid "Uppdatera", statusraden "Hämtat HH:MM · snapshot från HH:MM", Drift-raden "Kalender: senaste hämtning …" – alla fält valfria).


**Efterjusteringar (granskning 2026-09-17):** `config-push` tömmer även `busy:<datum>` för hela fönstret (`clearBusyCacheWindow`) eftersom posterna lever 11 min och bär konfigberoende innehåll (kalendrar/läge, restidFor, cooldownMin, preliminära); generationsstämpeln `preview:gen` läses FÖRE config-/ICS-läsningen i både triggern och `calendar-preview`; alla kalenderläsningar via `computeAvailability`/`previewBerakna_` skriver `busy:<datum>` med 11 min (en live-läsning är lika färsk som triggerns – annars skrevs triggerns poster över med 60 s efter varje cache-miss). Appen förbrukar sin "egen ändring → farsk"-flagga först när den tvingade hämtningen lyckats.

## Version 11 – blocksynk till Google Kalender (SCRIPT_VERSION 11)

CJ:s beslut 2026-09-17 (A61): *"Samtliga block ska synkas med Google-kalendern."* Modulen räknar redan fram restidsben (`previewResor` → `resor[].delar`) och marginalen efter varje möte (`BusyItem.cooldownMin` = effektiv buffert, version 10). Sedan version 11 skrivs dessa block som händelser i en **egen** Google-kalender **"Pipeline – restid"** som scriptet skapar första gången, så att CJ ser dem i Google Kalender (och kan färga/dölja kalendern). Modulen **läser aldrig** den kalendern – annars blev blocken hinder/ankare = dubbelräkning. Inga gäster (inget når Outlook), ingen beskrivning, inga kontaktuppgifter.

**Inställning (K1):** `installningar.blockSynkAktiv` (boolean, default `false`) – kryssrutan "Synka restid och marginal till Google Kalender" under Inställningar › Kalendrar; pushas med config som förut. Kalender-id:t ägs av **scriptet**: Script Properties `BLOCK_KALENDER_ID` och `BLOCK_SYNK_SENAST` (tabellen ovan). Appen skriver aldrig kalender-id:t.

**Kalendern (K2):** första körningen med `blockSynkAktiv && !BLOCK_KALENDER_ID` → `Calendar.Calendars.insert({ summary:'Pipeline – restid', timeZone:'Europe/Stockholm' })` (`blockKalenderSakerstall_`). Varje körning gör `Calendar.Calendars.get(id)`: 404 (kalendern borttagen av CJ) → ny kalender + omskriven property; annat fel → befintligt id behålls. Är synken av och kalendern borta rensas propertyn. `handleSetup`/`purge`/`dailyMaintenance` rör aldrig kalendern.

**Block (K3, `syncBlockBerakna_`):** fönstret är `[idag, horisontTom]` (samma horisont som bokningen, `horisontVeckor`, **inte** calendar-previews +7). Samma kedja som `calendar-preview`: `buildBusyList` **utan reservationer** (de lever 5 min och skulle flappa) → `geokodaAnkare` (högst `AVAIL_PREVIEW_MAX_GEO` = 60 nya Geocoding-anrop per körning) → `previewResor` med `travelSecondsForPairs_` (högst `AVAIL_PREVIEW_MAX_PAR` = 60 nya par; resten `okand` den här körningen och rätt nästa – cache-träffar kostar inget, så i drift är det bara nya adresser/par som kostar).
- **restid** – ett block per resa **och del** (`resor[].delar`): `{ typ:'restid', datum, start:'HH:MM', slut:'HH:MM' }`, titel `🚗 Restid från bas → X` / `X → bas` / `X → Y` + ` (N min)` med tillägget `, schablon` resp. `, okänd` efter status, och `⚠ ` först vid `konflikt`. `X` = händelsens titel: kundnamn ur inkorgen (annars `Bokning`) för modulens bokningar, `summary` för privat/ICS – `< >` strippade, ≤ 60 tecken (`BLOCK_TITEL_MAX`).
- **marginal** – ett block per räknad, tidsatt post (ej ignorerad, ej heldag) med `cooldownMin > 0` som slutar före 24:00: `{ typ:'marginal', datum, start = slut, slut = min(24:00, slut + cooldownMin) }`, titel `⏱ Marginal efter X (N min)`.
- **Nyckel:** `typ|datum|start|slut|ref` där `ref` = `franId>tillId` (restid) resp. `busy.id` (marginal) – deterministisk, ≤ 250 tecken (`BLOCK_NYCKEL_MAX`, långa ICS-UID:n klipps), lagras i `extendedProperties.private = { pipelineBlock:'1', nyckel, typ }`. En oförändrad plan ger noll skrivningar.
- **Händelseresurs:** `start`/`end` som `dateTime` med `timeZone` (`'24:00'` → nästa dags 00:00), `colorId` `'11'` (restid, tomat) / `'8'` (marginal, grafit), `reminders:{ useDefault:false }`, `transparency:'opaque'`, **inga `attendees`, ingen `description`**.

**Avstämning (K4, `syncBlock_(opts)` → `{ ok, aktiv, in, bort, andrade, kvar, antal, kalenderId, fel, hoppad, upptagen }`):** befintliga händelser läses med `Calendar.Events.list(calId, { timeMin: idag−1, timeMax: horisont+8 (exklusiv), singleEvents, maxResults:2500, privateExtendedProperty:'pipelineBlock=1', fields:'nextPageToken,items(id,status,summary,start,end,extendedProperties/private)' })` med pageToken-loop (fönstret är något vidare än kontraktets horisont+1 så att block bortom en **krympt** horisont också städas). Diff på nyckel: saknade → `insert`; befintliga med annan `summary` → `patch`; befintliga utan motsvarande block, utan nyckel eller dubbletter → `remove`. Ordning bort → in → andrade (ett flyttat möte får sitt gamla block bort innan det nya läggs in). **Passerade block (före idag) rörs aldrig.** Skrivtak `opts.maxSkriv` (trigger `BLOCK_SYNK_MAX_SKRIV_TRIGGER` = 40, endpoint `BLOCK_SYNK_MAX_SKRIV_ENDPOINT` = 80) **och** tidsbudget `opts.maxMs` (trigger 240 s, endpoint 30 s – appens fetch avbryter efter 45 s); `kvar` = ej utförda skrivningar (nästa körning tar dem – första fyllningen av 6 veckor sker alltså i portioner om 40 var 10:e minut, eller 80 per "Synka nu"). Varje Calendar-anrop i try/catch: fel räknas i `fel` (→ `ok:false`) utan att avbryta körningen; 5 fel i följd (`BLOCK_SYNK_MAX_FEL_I_FOLJD`) avbryter, resten = `kvar`. 404/410 vid `remove` = redan borta (räknas som utförd); vid `patch` = händelsen är borta (varken fel eller utförd – nästa körning lägger in den); vid `insert` = kalendern försvann mitt i körningen → `fel` + avbrott utan fler anrop (nästa körning skapar en ny kalender). **Ofullständig källa → `hoppad`:** är inkorgen oläsbar (`hoppad:'inkorg'` – kundnamnen skulle bli `Bokning` och titlarna flappa) eller Outlook-flödet oläsbart **utan** reservkopia i cache-filen (`hoppad:'outlook'` – alla Outlook-block skulle raderas och läggas in igen när flödet är tillbaka) görs inga skrivningar den körningen (`ok:false`, `kvar = antal`); med reservkopia (`readIcs.kalla 'reserv'` med `hamtadTs`) synkas planen som bokningen själv använder. **Samtidig körning:** CacheService-flaggan `blocksynk:pagar` (TTL = tidsbudget + 60 s, tas bort i `finally`) gör att "Synka nu" under en pågående triggerkörning svarar `upptagen:true` utan att skriva (ingen `LockService` – det låset skulle blockera `reserve`/`book` i upp till 4 minuter); dör körningen hårt ligger flaggan kvar högst TTL:n. `BLOCK_SYNK_SENAST` skrivs efteråt. **Synken av:** finns ett kalender-id tas alla `pipelineBlock`-händelser i fönstret bort (samma tak) – kalendern själv och passerade block ligger kvar; är fönstret tomt sätts CacheService `blocksynk:stadad` (6 h) så att triggern inte listar i onödan. Utan kalender-id görs inget. Loggrad `{ trigger:'syncBlock', ok, ms, in, bort, andrade, kvar, antal, fel, hoppad }` – aldrig titlar.

**Trigger och endpoint (K5):** `refreshIcsCache` (var 10:e minut, oförändrad trigger) kör `syncBlock_({ maxSkriv: 40 })` **efter** ICS-läsningen i egen try/catch (ICS-delen är utbruten till `refreshIcsLas_`), bara när `blockSynkAktiv` eller när ett kalender-id finns (städning efter avstängning); ICS-radens `ok`/`kalla` påverkas aldrig. Admin-endpointen **`block-sync`** `{ adminKey }` → `syncBlock_({ maxSkriv: 80 })` + `kalenderNamn:'Pipeline – restid'` ("Synka nu" i appen). Inget `LockService`-lås (inkorgen läses via cache, skrivningarna sker i modulens egen kalender) – bara pågår-flaggan `blocksynk:pagar` (se K4). `ping` får `blockSynk: { aktiv, kalenderId, senast: BLOCK_SYNK_SENAST-objektet | null }`.

**Skydd mot dubbelräkning (K6):** `readBusy` hoppar över kalendern vars id === `BLOCK_KALENDER_ID` oavsett läge (`kalBlockKalenderId_`); `normalizeGoogleEvent` returnerar `[]` för händelser med `extendedProperties.private.pipelineBlock === '1'`; `calendars-list` markerar kalendern med `blockKalender:true` (appen visar "Pipeline – restid – skapad av modulen, läses aldrig" utan läge-val); `config-push` varnar "Kalendern \"Pipeline – restid\" skapas av modulen och läses aldrig – läget ignoreras" om ett läge ändå sparats på den, och räknar den inte som bokningskalender ("Exakt en kalender ska ha läget Bokningar (fullt) – nu 0"); `bokningarKalenderId` utesluter den oavsett läge, så bokningar kan aldrig skrivas till en kalender som inte läses (`E_CALENDAR` i stället för dubbelbokning). ICS-vägen: `normalizeIcsItem` returnerar `[]` för händelser vars titel börjar med modulens egna blockprefix (`🚗 Restid `, `⏱ Marginal efter `, ev. med `⚠ ` först – `KAL_BLOCK_TITEL_RE` i Calendar.gs), så att blocken aldrig kommer tillbaka som hinder om Outlook någon gång skulle spegla kalendern "Pipeline – restid".

**Kostnad i drift:** per triggerkörning med synken på: `Calendars.get` + `Events.list` på blockkalendern + `readBusy` (en `Events.list` per läst kalender, som calendar-preview) + de skrivningar diffen kräver (normalt 0–5). Geokodning/Distance Matrix träffar cachen (CacheService 6 h + cache-filen), så bara nya adresser/par räknas mot `MAPS_DAILY_CAP`.

**Test:** `runAvailabilityTests()` oförändrad (88 test); blocksynken verifieras i Node med stubbar för Calendar/Properties/Cache/Drive (`v11_blocksynk_test.js` i arbetsmappen, 63 test: kalendern skapas, nycklar/titlar/tider ur ett syntetiskt busy med två fysiska möten + två Teams-möten + bas-ben, diff insert/patch/remove, skrivtak/tidsbudget/felgräns, avstängd synk städar, borttagen kalender, K6-skydd, `ping.blockSynk`, `block-sync`, `refreshIcsCache`-ordningen, inga titlar i loggrader) samt `v11_fix_review_test.js` (30 test: 404 vid insert/patch, `hoppad` för oläsbar inkorg/Outlook utan och med reservkopia, `upptagen`-flaggan, `bokningarKalenderId`/`configVarningar` mot blockkalendern, ICS-titelskyddet).

Deploy: New version; **ingen `install()`** (manifestet oförändrat – ingen ny auktorisering); `?action=ping` → `scriptVersion: 11`. Ordning som vanligt: script (Ny version) → appen (fältet och knappen är valfria).

## Version 10 – ren restid, marginal efter möte, restid delad runt platslösa möten (SCRIPT_VERSION 10)

CJ:s beslut 2026-09-17 (ersätter spec 5.6 och 5.8): *"Det finns luft i Googles restid."* Restiden är därför **Googles råa körtid** – Distance Matrix `duration`, `ceil(sek/60)`, **inget påslag** (varken "minst 15 min", 25 % eller 5-minutersavrundning; `travelMinutesRaw`, aliaset `travelWithMargin` ger samma värde). Schablonen (`schablonRestidMin`, default 45) används som förut när Maps saknas/adressen inte geokodats; "för långt" (fågelväg > 150 km) = fågelväg/70 km/h i råa minuter – obs: den uppskattningen är minst 129 min (150 km/70 km/h), så garantin "för långt ⇒ dold (maxRestid)" gäller bara när `maxEnkelResaMin` < 129; med en högre gräns kan ett "för långt"-par visas som ledig lucka utan Maps-anrop (version ≤ 9 gav 165 min med påslag). Marginalen läggs i stället **efter varje möte**, och resan **delas runt platslösa möten** i stället för att kräva ett sammanhängande hål.

**Inställningar (`config.installningar`, pushas av appen via `config-push`):**

| Fält | Default | Betydelse |
|---|---|---|
| `marginalFysisktMin` | 15 | Marginal (min) efter ett fysiskt möte (restidsankare) |
| `marginalOnlineMin` | 5 | Marginal (min) efter ett online-/platslöst möte (Teams m.fl.) |
| `schablonRestidMin` | 45 | Oförändrad – restid när Maps saknas/adressen inte tolkats |
| ~~`marginalMinstMin`~~, ~~`marginalProcent`~~ | – | **Borttagna** ur defaults och appens UI; ignoreras om de ligger kvar i en äldre config-fil |

**Effektiv buffert** efter ett möte = `max(mötestypens cooldownMin [bara modulens bokningar/reservationer], fysiskt ? marginalFysisktMin : marginalOnlineMin)`. Den gäller **alla** tidsatta, icke-ignorerade händelser (Google privat/fullt, Outlook-ICS, inkorgsbokningar, reservationer); heldag = 0. Ordningen möte → buffert → resa är oförändrad.

**Kontrakt K1–K7 (kort):**
- **K1 – `placeTravelDelar(gapStart, gapEnd, obstacles, minutes, side)` → `{ delar:[[s,e],…] kronologiskt, rackte }`** (Availability.gs): `'in'` går igenom de fria intervallen bakifrån (närmast mötet först) och tar `min(kvar, längd)` från slutet av varje; `'ut'` framifrån från starten; `rackte = kvar === 0`; inga minimilängder; `minutes 0 → { delar:[], rackte:true }`. `placeTravel` är kvar som wrapper (yttre spannet eller `null`). Exempel: Teams 09–10, Teams 11–12, fysiskt möte 14:00 med 130 min från bas → `12:05–14:00` + `10:45–11:00` (hindren inkluderar Teams-marginalen 5) – inte 06:50–09:00.
- **K2 – `mapCfg`/`dayPlan`/`computeAvailability`:** `cfg.restid = { schablonMin }`, nytt `cfg.marginal = { fysisktMin, onlineMin }`. Den nya luckans buffert `C = luckBuffertMin(cfg, typ) = max(typ.cooldownMin, typ.restid ? fysisktMin : onlineMin)` (hinder `[S, S+L+C]`, utresan börjar `S+L+C`) – gäller även Teams-typer (cooldown 0 → 5 min). `slot.restid.inBlock`/`utBlock` = **yttre spannet** (kan nu spänna över platslösa händelser), nytt `slot.restid.inDelar`/`utDelar = [['HH:MM','HH:MM'], …]` (utelämnas när tomt); ryms inte hela restiden → status `restid` som förut. Nytt `data.pausMin = C` (bokningssidan ritar vald luckas marginal `S+L → S+L+pausMin`). `blockFor` oförändrad i form – paus-block för alla händelser med `cooldownMin > 0`, vilket nu är alla tidsatta möten. `previewResor`: `minuter` råa (även "för långt"), nytt `delar:[{ start, slut }]` kronologiskt, `start`/`slut` = yttre spann, `konflikt = !rackte` (då `delar` = ett block närmast mötet som förut). Inga `raMin`/`marginalMin`-fält.
- **K3 – Calendar.gs `finalizeBusy`:** `item.cooldownMin = heldag ? 0 : max(cooldownMin, isTravelMeeting ? marginal.fysisktMin : marginal.onlineMin)` för alla poster (`opts.marginal = kalMarginal_(config)`, defaults 15/5). Därmed får `dayPlan` (hinder, prev-gräns), `blockFor` (paus-block), `previewResor` (gapStart efter buffert) och `previewExport` (`handelser[].cooldownMin`) den effektiva bufferten utan andra ändringar.
- **K4 – Code.gs/README:** `DEFAULT_BOKNINGSINSTALLNINGAR` med `marginalFysisktMin: 15, marginalOnlineMin: 5` (utan `marginalMinstMin`/`marginalProcent`); `SCRIPT_VERSION = 10`; notismejlets restidsrad lyder `ok (X min före, Y min efter)` (utan tillägget om marginal).
- **K5 – appen:** inställningsfälten "Marginal efter fysiskt möte (min)" / "Marginal efter online-möte (min)"; kalendervyn ritar `resor[].delar` som separata block och pausremsan som "Marginal N min".
- **K6 – bokningssidan:** ritar `inDelar`/`utDelar` (fallback `inBlock`/`utBlock`), vald luckas marginal ur `data.pausMin` (fallback `motestyp.cooldownMin`), paus-block etiketterade "Marginal". Bokaren får som förut aldrig råa restidsminuter utöver blocken (5.12).
- **K7 – `runAvailabilityTests()`:** 88 test, bl.a. råa minuter (1200 s → 20, 1920 s → 32, 0 → 0), `placeTravelDelar`-exemplet (A slutar 09:00 +30, Teams 11:00–11:45, S 12:00, 40 min → `[[10:35,11:00],[11:45,12:00]]`), CJ:s exempel i `previewResor`, konflikt-block, delat `'ut'`-ben, `inDelar` med två delar i `dayPlan`, `data.pausMin`, paus-block 15/5 för Outlook/privat-händelser.

Deploy: New version; ingen `install()` behövs; `?action=ping` → `scriptVersion: 10`. Ordning som vanligt: script (Ny version) → sajten → appen (de nya fälten är valfria, så ordningen spelar ingen roll för funktionen – men restidssiffrorna sjunker i alla klienter så snart scriptet är ute).

## Version 9 – manuell restidsklassning och rättad adress per händelse (SCRIPT_VERSION 9)

Automatiken (`kalLooksLikePlace`/`KAL_ONLINE_RE` för kalenderhändelser, mötestypens restid-flagga för modulens bokningar) avgör om en händelse är restidsankare, och Google måste hitta platstexten för att restiden ska räknas. CJ kan nu, per händelse i Kalenderkoll (appens detaljpanel), **klassa mötet manuellt som online-möte (inget ankare) eller fysiskt möte (ankare)** och **rätta/lägga till adressen som används för restiden** – manuellt vinner alltid över automatiken. Allt lagras på samma `KALENDER_IGNORERA`-post (spec 3.5) som Ignorera/Räkna och når scriptet via `config-push` som `config.ignorerade[]`; ingen ny endpoint.

**`KALENDER_IGNORERA`-post (`config.ignorerade[]`):** `{ id, kalla:'privat'|'bokningar'|'ics', lage:'ignorera'|'rakna'|'restid', online?: true|false, adress?: string (≤ 200), lat?: number, lng?: number, omrade?: string (≤ 60), ts }`. **En post per händelse-id** – Ignorera/Räkna och online/adress samsas i samma post; `lage:'restid'` = posten bär *bara* restid-override (varken ignorera eller räkna). Matchning som förut på `item.matchIds` (Google event-id, `recurringEventId`, ICS UID). **Räckvidd för återkommande möten:** en post med Googles instans-id (`<serie>_<tid>Z` = `calendar-preview`-postens `ignoreraId`) träffar **bara den instansen**; en post med `recurringEventId` (`calendar-preview`-postens nya `serieId`) eller ICS UID (delas av seriens alla instanser) träffar **hela serien** – appen lagrar därför restid-poster med `id = serieId` när det finns (Ignorera/Räkna kan förbli per instans). `normalizeConfig` (`normalizeIgnoreradPost`) sanerar de nya fälten när config-filen läses: `online` bara boolean, `adress` `cleanText` ≤ 200, `lat`/`lng` bara finita tal och bara tillsammans med `adress`, `omrade` ≤ 60 och bara tillsammans med koordinater – ogiltiga fält utelämnas tyst (posten behålls, ett trasigt adressfält slår aldrig ut ett Ignorera/Räkna).

**Tillämpning (Calendar.gs `applyIgnore`, efter `mergeBusy`, före `finalizeBusy` – samma steg som förut):**
- `lage`: bara exakt `'ignorera'` resp. `'rakna'` räknas; `'restid'`, saknat, tomt och okända värden gör varken ignorera eller räkna (till och med version 8 tolkades saknat `lage` som `'ignorera'`). Gamla poster utan `lage` (3.5-migreringen) får `'ignorera'` av appens uppstartsnormalisering innan de pushas.
- `adress` med giltiga `lat`/`lng` → `plats = { text: adress, lat, lng, geokodad:true, omrade? }` och `hasPlace:true` (om inte `online === true`); `adress` utan koordinater → `plats.text` byts, `geokodad:false` (`geokodaAnkare` geokodar texten som förut, samma cachekedja/kostnadstak), `hasPlace:true` (dito). Den ursprungliga platstexten/`omrade` ersätts.
- `online === true` → `hasPlace:false` – vinner över allt (även `adress` och mötestypens flagga). `online === false` → `hasPlace:true` när platsen har text eller koordinater (åsidosätter `KAL_ONLINE_RE` – "Microsoft Teams-möte" med `online:false` blir ankare – och `restidFor`); är platstexten ett online-ord/URL (`kalLooksLikePlace` false) utan rättad adress **töms `plats.text`**: ankaret får schablon (5.13) i stället för att `geokodaAnkare` slår upp "Microsoft Teams-möte" hos Google (ett felankare någonstans i landet vore värre än schablon) – Kalenderkoll visar då tom `plats`, rätta adressen; helt platslös händelse förblir utan ankare (lägg till adress).
- `finalizeBusy`:s Teams-skyddsnät (`restidFor(motestypId) === false` → `hasPlace:false`, version 7) hoppar över poster med override `online === false` eller `adress` (internt fält `item.override = { online, adress }` – exporteras aldrig till bokaren; `availability` skickar bara block).
- `isTravelMeeting = hasPlace && !ignore && !heldag` som förut – en ignorerad post med `online:false` är fortfarande inget ankare.

**`calendar-preview`** får per händelse de valfria fälten `overrideOnline: true|false|null` och `overrideAdress: ''|text` (rättad adress, ≤ 200 tecken – visas även för `kalla:'privat'` eftersom det är CJ:s egen inmatning; `plats` förblir `''` för läge `tider`), så att appen kan visa "manuellt"/"✎" och knappen "Återställ automatik", samt `serieId: ''|text` = Googles `recurringEventId` när händelsen är en instans av en återkommande Google-händelse (annars `''`, även för ICS där UID:t redan täcker serien) – det id appen lagrar på restid-poster så att klassningen gäller hela serien. `plats`/`omrade`/`hasPlace`/`restid` speglar det överstyrda värdet.

**`geocode`** (`k` eller `adminKey`) svarar utöver `{ status, lat, lng, formaterad }` även `omrade` (områdesetiketten `'Stad · Stadsdel'` när geokodningen gav en, annars `''`) – appens "Adress för restid" sparar `formaterad` (fallback inmatad text), `lat`, `lng` och `omrade` på posten; `status:'okand'` → appen sparar inget. Samma gränser och cache som förut.

Deploy: New version; ingen `install()` behövs (inga nya triggers/scopes); `?action=ping` → `scriptVersion: 9`. Node-test av `applyIgnore`/`finalizeBusy`/`normalizeIgnoreradPost`/`previewExport` kördes vm-baserat vid införandet (33 fall); `runAvailabilityTests()` opåverkad.

## Version 8 – online-möten är aldrig restidsankare (SCRIPT_VERSION 8)

Outlook sätter `LOCATION` "Microsoft Teams-möte"/"Microsoft Teams Meeting" (ibland "Konferensrum X; Microsoft Teams-möte") på Teams-inbjudningar; det gamla filtret i `kalLooksLikePlace` fångade bara texter som *börjar* med teams/zoom/…, så sådana möten geokodades till en riktig plats och blev restidsankare med helt felaktiga ben ("145 min från bas" före ett Teams-möte, 195 min till nästa möte). Nu (`KAL_ONLINE_RE`) är en plats som innehåller ett online-ord var som helst i texten – microsoft teams, teams, zoom, google meet, webex, skype, telefon(möte), online, digitalt, distans, videomöte/-länk, virtuellt – aldrig ett ankare (ordgränser: "Telefonvägen 3" och "Distansgatan 5" är fortfarande platser). Bara `LOCATION`-texten bedöms – en riktig gatuadress med Teams-länk i beskrivningen förblir ett ankare (hellre ett ankare för mycket än en missad resa). Gäller Outlook-ICS och Google-kalendrar; modulens egna bokningar styrs av mötestypens restid-flagga (version 7). Gamla geokodposter för sådana texter i cache-filen är ofarliga (används aldrig, gallras efter 180 dagar). Deploy: New version; ingen `install()` behövs (inga nya triggers/scopes); `?action=ping` → `scriptVersion: 8`.

## Version 7 – optimering, restid tydlig, Teams-fix (SCRIPT_VERSION 7)

**Optimering (färre Drive-/UrlFetch-omgångar per anrop):**
- **Inkorgen i CacheService (6 h).** Scriptet är ensam skrivare av `telexia-bokning-inbox.json` (appen läser den bara via `inbox-list`), så en cachad kopia är koherent så länge **bara låshållaren fyller cachen** (4.8): `writeInbox` (1) invaliderar, (2) skriver Drive, (3) lägger det nya innehållet i cachen och dess `rev` i Script Property `INBOX_REV` (best effort – misslyckas steg 3 läser nästa anrop Drive). En läsning **utan** lås (`hello`/`availability`/`calendar-preview`/`inbox-list` …) får cache-träffar men fyller aldrig cachen – den kan läsa Drive mitt i en skrivning (efter steg 1, före steg 2) och hade annars planterat den gamla kopian efter skrivarens steg 3, varpå nästa läs-ändra-skriv under låset skrivit bort den nya bokningen (`readInbox` fyller bara när `withScriptLock` håller låset, flaggan `LAS_HALLS_`). Skyddsnät nr 2: en cache-träff godtas bara när kopians `rev` == `INBOX_REV`, annars Drive – täcker även att steg 1 och 3 tyst misslyckas (raderade poster hade annars kunnat återuppstå efter `purge`/gallring). Format: JSON → gzip → base64 → bitar ≤ 90 KB under `inbox:<fileId>:<n>` + indexnyckeln `inbox:<fileId>` `{ v:1, delar, langd, rev, ts }`; fler än 8 bitar → ingen cache. `readInbox` faller tillbaka på Drive vid trasig/ofullständig cache. Ingen per-körning-memo av objektet (anroparna muterar det). Cachen rensas av `setup`/`clearSetupCache`/`seedBrevladaFile`. Sparar Drive-läsningen (~0,3–1 s) i `hello`/`availability`/`reserve`/`book`/`calendar-preview`/`egen-*`.
- **Drive-filobjektet memoiseras per körning** (`BREVLADA_FIL_MEMO`, nollställs i `doPost`/`dailyMaintenance`/`refreshIcsCache`): `DriveApp.getFileById` en gång per fil och körning i stället för vid varje läsning och skrivning. `verifyBrevladaFile` (`setupok:`, 10 min) oförändrad.
- **En cache-filskrivning per körning:** nya restidspar (Distance Matrix) samlas i `AVAIL_RESTID_PENDING` bredvid geokodposterna (`AVAIL_GEOKOD_PENDING`) och skrivs i **samma** läs-ändra-skriv av `availFlushGeokod_` (namnet behålls) i `doPost`/`dailyMaintenance`/`refreshIcsCache` (finally – pending tappas aldrig). `backfillOmrade_`/`gallraCacheFil`/`writeIcsReserv` använder `updateCacheFile` direkt som förut.
- **ICS-värmare `refreshIcsCache`** (tidsstyrd trigger var 10:e minut, skapas av `install()`): läser `ics:meta` – är `hamtadTs` yngre än 9 min och `ics:busy`-indexet finns hoppas körningen över, annars `readIcs(config, { farsk:true })`. Utan `outlookIcsUrl` gör den inget. Resultat: bokningssidans/appens anrop gör praktiskt taget aldrig `UrlFetchApp` mot Outlook själva (den största posten vid cache-miss). Kvot: 144 hämtningar/dygn (UrlFetch-kvoten är 20 000/dygn), trigger-runtime ≈ några sekunder per körning (gränsen är 90 min/dygn). Fel fäller aldrig; en loggrad `{ trigger:'refreshIcsCache', ok, ms, kalla }` utan personuppgifter.
- **`book` värmer inte längre dagsberäkningen före låset** (`warmSlotCaches` bara i `reserve`): geokodning + uttrycklig `readIcs` före låset räcker – allt annat värmdes av `reserve` ≤ 5 min tidigare (CacheService `geo:`/`restid:` 6 h), den färska beräkningen under låset avgör ändå, och uppvärmningen kostade en kalenderläsning + en inkorgsläsning per bokning.

**Restid tydlig (kontrakt K1–K5 med appen och bokningssidan):**
- **K1 – paus-block:** `availability` `dagar[].block[]`: `upptaget` slutar vid mötets **slut** (inte slut + cooldown); har källan `cooldownMin > 0` följer `{ typ:'paus', start, slut }` direkt efter (samma `egen`/`kundnamn`/`bokningId` som upptaget-blocket om egen; aldrig `omrade` på paus). Lunch/heldag oförändrade, sortering på start. Hindret i `dayPlan` är oförändrat (möte + cooldown). Äldre klienter ignorerar den okända typen.
- **K2:** `calendar-preview.handelser[].cooldownMin` fanns redan – appen ritar pausremsan.
- **K3 – samma koordinater = 0 min** utan marginal: `buildTravelTable` ger `{ min:0, kalla:'maps' }` (inBlock/utBlock tomma, `restidMin.fore/efter` 0) och `previewResor` **utelämnar** benet ur `resor`.
- **K5 – mejl:** notismejlets restidsrad lyder sedan version 10 `ok (X min före, Y min efter)` (version 7 hade ett tillägg om marginal – minuterna är nu råa).
- **Teams-fix (mötestyp utan restid blir aldrig restidsankare):** `bookingEventResource` sätter `location` bara när `typ.restid === true` – annars tom `location` och adressen som raden `Adress: …` i beskrivningen (gäller även `rebook`/`egen-update` via samma resurs; `patchBookingEvent` patchar beskrivningen när adress/typ ändrats). Calendar.gs: `kalRestidFn_(config)` → `motestypId → typ.restid === true` (okänd typ → `true`) skickas som `opts.restidFor` till `normalizeGoogleEvent` (`'bokningar'` med `extendedProperties.private.motestypId`), `inboxBookingToBusy` och `finalizeBusy` (fångar Outlooks ICS-kopia av en äldre Teams-bokning med `LOCATION`, som vinner sammanslagningen på `hasPlace`) → `hasPlace:false` (plats/`omrade` kvar för visning, inget ankare).
- **`rebook` släpper reservationen:** efter lyckad flytt under låset släpps den honorerade reservationen (`rebookReservationId`), best effort – luckan ligger inte kvar som hinder i upp till 5 min.

## Områdesetikett och restid i Kalenderkoll (steg 2c, SCRIPT_VERSION 6, A56–A57)

**Områdesetikett (`omrade`).** Geokodningen (`geocodeViaApi`) härleder ur Geocoding-svarets `address_components`: `locality` (annars `postal_town`) → `stad`; `sublocality_level_1` (annars `sublocality`, annars `neighborhood`) → `stadsdel`; `etikett = stad + ' · ' + stadsdel` (bara staden när stadsdel saknas eller är samma som staden; tomt när inget hittas). Objektet `omrade = { stad, stadsdel, etikett }` följer med genom hela cachekedjan (körningens memo, CacheService `geo:`/`geo:pid:`, cache-filens `geokod[nyckel].omrade`) och landar som **`plats.omrade` (etiketten, ≤ 60 tecken, rensad) på BusyItem** (5.2) när platsen är geokodad: inkorgspostens `geo.omrade` (sätts av `book`/`rebook`/`egen-update`/`egen-rebook` – bara när etiketten finns), reservationens `plats.omrade` (`reserve`) och `geokodaAnkare` för platstexter ur kalendrar/ICS. **Bakåtkompatibelt:** äldre geokodposter utan `omrade` ger etikett `''` tills `dailyMaintenance` (steg 2b i loggraden, `omrade:N`) geokodat om dem – högst `MAINT_BACKFILL_OMRADE_MAX` = 50 poster per natt (Googles formaterade adress, annars nyckeln; Geocoding-element mot `MAPS_DAILY_CAP`, stopp vid tak/block/nyckelfel; `ZERO_RESULTS` → tomt `omrade` så att posten inte försöks igen; ett enstaka annat fel hoppar över posten – efter `AVAIL_BACKFILL_FEL_MAX` = 3 sådana i rad avbryts natten; poster märkta `gallrad:true` och `status:'okand'`-poster backfillas aldrig). **`ZERO_RESULTS` i cache-filen:** en adress/kalendertext Google tolkar men inte hittar (t.ex. `Konferensrum 3`) sparas som `geokod[nyckel] = { status:'okand', ts }` och ger `'okand'` utan nytt anrop i `AVAIL_GEO_OKAND_FIL_DAGAR` = 30 dagar (gallringen tar bort posten efter 180 dagar) – annars kostade varje otolkbar kalendertext ett Geocoding-element per timme (CacheService-memot är 1 h).

**Till bokaren (`availability`, 5.12 – läckageregel):** varje `block` av typ `upptaget` som kommer från en händelse med geokodad plats får fältet `omrade` (etiketten) – även egna bokningar och reservationer (andras reservationer om de är geokodade) och händelser i kalendern Bokningar (`fullt`) eller Outlook-ICS som bara har platstext. **Privat kalender i läge `tider` (`kalla:'privat'`) → aldrig `omrade`** – läget exporterar bara tider, platsen används enbart för restidsberäkningen (till CJ i `calendar-preview` visas etiketten även för privat). Ankarna geokodas i `availability` oberoende av mötestyp och av om bokarens adress tolkats (samma cachekedja som `calendar-preview`; nya platstexter räknas mot `MAPS_DAILY_CAP`), så etiketten är densamma på samma block oavsett vilken mötestyp bokaren tittar på. **Inget annat nytt läcker:** inte titel, adress, koordinater, källa eller råa restidsminuter. Blocket ser ut som `{ typ:'upptaget', start, slut, egen, omrade? }` (+ `kundnamn`/`bokningId` på egna); sedan version 7 slutar `upptaget` vid mötets slut och cooldownen är ett eget block `{ typ:'paus', start, slut, egen }` direkt efter (K1, aldrig `omrade`); sedan version 10 är pausen den effektiva bufferten (cooldown eller marginal 15/5), så alla tidsatta möten får ett paus-block, och lediga luckor får `restid.inDelar`/`utDelar` (restiden delad runt platslösa möten; `inBlock`/`utBlock` = yttre spannet) samt svaret `pausMin` (vald luckas marginal). Sidan visar etiketten som liten text i blocket (SV/EN oberoende – ett ortnamn).

**Till appen (`calendar-preview`):** `omrade` per händelse och `resor` per dag – se tabellen under Admin-endpoints. Kalender-underfliken ritar resorna som rödrandiga block ("⇄ 35 min", "från bas"/"till bas"), `konflikt:true` → röd kant + varning, och Kalenderkoll-tabellen får kolumnen Restid.

## Bokar-endpoints `egen-*` (steg 2a, CJ:s beslut 2026-09-15) – anropas av bokningssidan med bokarkod `k`

Bokaren får **omboka**, **avboka** och **ändra uppgifter** på sina **egna** bokningar (`bokareId === bokare.id`) ända fram till mötets starttid. CJ meddelas per e-post (`notisEpost`) vid varje åtgärd; kunden meddelas av bokaren själv (som vid bokning). **MailApp-kvot:** ett vanligt Google-konto får skicka till ~100 mottagare per dag; `skickaCjMejl` (och bokarmejlen i `ack`/`rebook`/`cancel`) kontrollerar `MailApp.getRemainingDailyQuota()` och loggar historik `{ typ:'mejlfel', av:'script', till:'cj' }` utan sändningsförsök när kvoten är slut (anropet lyckas ändå). Appens spegling lägger då loggposten "Notismejlet till CJ kunde inte skickas …" på processen, så att ÄNDRAD-markören i Inkorg inte är enda signalen. `MAX_ANDR_GLOBAL_D` (40/dag) begränsar dessutom bokarnas ändringsmejl globalt. Outlook uppdateras via `sendUpdates:'all'` precis som i admin-`rebook`/`cancel`. Kärnorna delas med admin-varianterna (`rebookForbered`/`rebookUtfor`, `cancelUtfor`, `validateBookFalt`, `patchEllerSkapa`) – ingen kopierad logik.

**Gemensamt för alla tre:** kod → `E_KEY` (anropsgränser som övriga bokaranrop); `bokningId` (36 tecken) → `E_VALIDATION falt.bokningId`; posten måste tillhöra bokaren – **annan bokares eller okänd post → `E_NOT_FOUND`** (aldrig `E_ADMIN`/`E_STATE`: svaret avslöjar inte att posten finns; CJ-bokare får också bara sina egna); status `ny`/`importerad` (annars `E_STATE` med `details.status` = egna-status `avbokad`|`avvisad`); `start > nu` (annars `E_STATE` med statisk text **"Mötet har redan börjat"** och `details.passerad:true`). Kontrollen görs före låset (validering/cache-värmning) och på nytt under låset. Handlingsgräns per kod: `MAX_ANDR_PER_KOD_H` 5 / `MAX_ANDR_PER_KOD_D` 15 ändringar sammanlagt (`E_RATE` typ `andringar`, egna räknare – `ping.bokningarIdag` påverkas inte; misslyckade anrop räknas inte). Svar: `{ bokning: <egna-format, se hello nedan>, rev: <postens rev efter ändringen>, kalenderFel }` (+ `andrade:[…]` i `egen-update`). Historikposterna får `av:'bokare'` (appen slår upp namnet via `bokareId`). `rev` stegas i alla tre (även avbokning) så att appens spegling (8.3) ser ändringen.

| Action | Request (utöver `k`) | `data` vid `ok:true` | Fel |
|---|---|---|---|
| `egen-rebook` | `{ bokningId, start, adress?, reservationId? }` – `start` som i `rebook` (ISO med offset eller `{ datum, tid }`; samma regler som `book`: raster, öppen dag, framförhållning per mötestyp, horisont; paus t.o.m. måldagen → `E_PAUSED`); `adress` = ny adress (typ med restid; utelämnad → befintlig; tom vid restid → `E_VALIDATION falt.adress`; ny adress – som skiljer sig från postens – räknas mot `MAX_ADRESSER_PER_KOD_D`/`MAX_GEOCODE_PER_KOD_H` och geokodas mot bokarens kod; en ren tidsflytt räknas inte); `reservationId` undantas bara om reservationen är bokarens egen; **bokningens egen mötestyp används alltid** (även inaktiv/inte längre tillåten) – `motestypId` i anropet ignoreras | `{ bokning, rev, kalenderFel:false }` – under lås: färsk luckkontroll med `computeAvailability(undantaBokningId = bokningId)` (sidan visar samma vy: `availability` med `undantaBokningId` honoreras för bokningens egen bokare) → `E_SLOT_TAKEN`; `Calendar.Events.patch(start/end/location, sendUpdates:'all')` (händelsen borta 404/410 → ny händelse; annat fel → `E_CALENDAR`, inget ändrat); posten `start/slut/adress/geo/restid`, `rev+1`, `andradAt`, historik `{ typ:'ombokad', av:'bokare', fran, till }` – **status oförändrad**; misslyckad inkorgsskrivning → händelsen flyttas tillbaka → `E_INTERNAL`; busy-cachen töms för båda dagarna; mejl till CJ "Ombokning: <mötestyp> <datum> <tid>" med tidigare/ny tid, bokare, kund, restidsrad (aldrig kontaktuppgifter/adress) | `E_NOT_FOUND`, `E_STATE`, `E_SLOT_TAKEN`, `E_PAUSED`, `E_VALIDATION` (`falt.bokningId/start/adress`), `E_RATE` (`andringar`/`adresser`/`geocode`), `E_CALENDAR`, `E_LOCK`, `E_SETUP` |
| `egen-cancel` | `{ bokningId, orsak? }` (orsak ≤ 500 tecken – sparas som `avbokadOrsak` + `historik.orsak` för Inkorg, **mejlas aldrig**) | `{ bokning, rev, kalenderFel }` – under lås: `Calendar.Events.remove(sendUpdates:'all')` (404/410 = redan borta = ok; annat fel → status sätts ändå + historik `kalenderfel` + `kalenderFel:true`), status `avbokad`, `avbokadTs`/`avbokadOrsak`/`andradAt`, `rev+1`, historik `{ typ:'avbokad', av:'bokare', orsak }`; posten raderas aldrig; busy-cachen töms; mejl till CJ "Avbokning: …" med tid, bokare, kund och `Orsak: angiven – se Inkorg i Pipeline` / `ingen angiven` (vid kalenderfel: "OBS: kalenderhändelsen kunde inte tas bort") | `E_NOT_FOUND`, `E_STATE`, `E_VALIDATION` (`falt.bokningId/orsak`), `E_RATE`, `E_LOCK`, `E_SETUP` |
| `egen-update` | `{ bokningId, kund:{ namn, orgnr }, kontakt:{ namn, telefon, epost }, adress?, notering?, extrafalt? }` – **hela uppsättningen som i `book`** (utelämnat fält = tomt); samma validering som `book` (`validateBookFalt`: maxlängder 3.5, orgnr-Luhn, orgnr obligatoriskt utom för CJ-bokare, adress tvingas vid restid, e-post utan restid, telefon ≥ 8 siffror när obligatorisk, extrafältens typregler, okänt extrafält → `falt.extrafalt` utan id-eko) → `E_VALIDATION details.falt` med alla fältfel i ett svar; `extrafalt._kundId` (CJ-bokare) bevaras om det inte skickas, liksom extrafält som CJ gömt (`synlig:false`) eller tagit bort ur formuläret efter bokningen (`bevaraDoldaExtrafalt` – sidan kan inte skicka dem, de räknas inte som ändrade och raderas inte) | `{ bokning, rev, kalenderFel:false, andrade:[…] }` – `andrade` = ändrade fält som **statiska namn** i ordningen `kundnamn, orgnr, kontaktperson, telefon, epost, adress, notering, extrafalt` (`extrafalt` en gång oavsett antal); **inget skiljer sig → `andrade:[]` utan lås, skrivning eller mejl (`rev` oförändrad)**. Ny adress på typ med restid: adress-/geokodgränser, geokodning och **omräknad restid** ur den färska luckan på bokningens tid (`undantaBokningId = bokningId`): rymmer luckan inte restiden → `E_SLOT_TAKEN` med statisk text "Tiden rymmer inte restiden till den nya adressen – omboka i stället" (inget ändrat); är dagen inte öppen i tillgängligheten (t.ex. inom framförhållningen) kan restiden inte verifieras → `restid.status:'okand'` med schablonminuter (CJ flaggas i mejl och app). Kalendern: `patchBookingDetails` – bara `summary`/`location`/`description` som faktiskt ändrats patchas (`sendUpdates:'all'`; händelsen borta → ny händelse med de nya uppgifterna; annat fel → `E_CALENDAR`, inget ändrat); posten `kund/kontakt/adress/notering/extrafalt` (+ `geo/restid` vid adressbyte), `rev+1`, `andradAt`, historik `{ typ:'andrad', av:'bokare', falt:[…] }` – status/start oförändrade; misslyckad inkorgsskrivning → beskrivningen patchas tillbaka → `E_INTERNAL`; mejl till CJ "Ändrade uppgifter: …" med `Ändrade fält: <namn>` (aldrig värden) och restidsrad | `E_NOT_FOUND`, `E_STATE`, `E_VALIDATION` (`falt.<fält>`), `E_SLOT_TAKEN`, `E_RATE`, `E_CALENDAR`, `E_LOCK`, `E_SETUP` |

**`hello.egna` (utökat i version 4):** varje post har `{ bokningId, start, slut, kundNamn, kontaktNamn, adress, motestypId, status:'bokad'|'ombokad'|'avbokad'|'avvisad', kund:{ namn, orgnr }, kontakt:{ namn, telefon, epost }, notering, extrafalt:{ <id>: <sträng> }, rev, kanAndras }` – `kanAndras` = status `bokad`/`ombokad` **och** `start > nu` (samma villkor som endpointsens `E_STATE`); sidan visar Omboka/Ändra/Avboka bara när `kanAndras`. Aldrig `geo`, `kalenderEventId`, `historik`, `plan` eller `importeradAv` (inget om CJ:s enheter/kalender når bokaren). M2-fälten (`kundNamn`, `kontaktNamn`, `adress`) finns kvar oförändrade.

**Sidans flöde (7.x):** Omboka → `availability { k, motestypId: <bokningens>, undantaBokningId: bokningId, adress }` (egen tid visas som ledig, bokningens typ accepteras även om inaktiv) → val av lucka → `egen-rebook { k, bokningId, start[, adress] }`; Ändra → formuläret förifyllt ur `egna[]` → `egen-update`; Avboka → orsak (valfri) → `egen-cancel`. Efter varje åtgärd: `hello` på nytt (eller `data.bokning` in i listan). Inget `reserve` i ombokningsläget – luckan avgörs av `egen-rebook` under låset. Adressfälten (steg 2, Ändra, ombokningens "Ändra adress") frågar `adressforslag` medan bokaren skriver och skickar valt `placeId` med i `geocode`/`availability`/`egen-rebook`/`egen-update`/`book` (steg 2b, se "Adressförslag" nedan).

**Appens spegling (8.3):** historik med `av:'bokare'` → loggpost "Ombokat av bokaren <namn>: gammal → ny" / "Avbokat av bokaren <namn>" / "Uppgifter ändrade av bokaren <namn>: <falt>"; `typ:'andrad'` bär `falt:[…]` – appen uppdaterar händelsens adress/notering och kund-/kontaktuppgifter försiktigt (nya värden finns i posten via `inbox-list`).

## Adressförslag `adressforslag` (steg 2b, autocomplete via Places API (New)) – `k` eller `adminKey`

Bokningssidans adressfält (steg 2, Ändra-formuläret, ombokningens "Ändra adress") och appens Inställningar › Basadress visar en förslagslista medan man skriver. Allt går via scriptet – klienterna talar aldrig direkt med Google och nyckeln lämnar aldrig Script Properties.

| Action | Request | `data` vid `ok:true` | Fel |
|---|---|---|---|
| `adressforslag` | `{ k \| adminKey, q, sessionToken? }` – `q` trimmad, högst 120 tecken (längre eller fel typ → `E_VALIDATION falt.q`); **kortare än 3 tecken → `{ forslag:[], kalla:'ingen' }` utan anrop och utan fel**; `sessionToken` = klientens `crypto.randomUUID()` per adressfält-session (`[A-Za-z0-9_-]{8,36}` – Googles gräns är URL-/filnamnssäker base64 med högst 36 tecken, annars `E_VALIDATION falt.sessionToken`) – skickas vidare till Places (sessionering, se kostnadsnoten), aldrig till Geocoding | `{ forslag:[{ text, placeId, huvud, detalj }], kalla:'places' \| 'ingen'[, varning:'places'] }` – högst 5 förslag; `text` = hela förslaget (fältet fylls med den), `huvud`/`detalj` = Places `structuredFormat.mainText/secondaryText` (listans två rader), `placeId` = Googles place-id (`[A-Za-z0-9_-]{10,300}`) som klienten skickar med i `geocode`/`availability`/`reserve`/`book`/`egen-update`/`egen-rebook`. **Ingen Maps-nyckel, `MAPS_DAILY_CAP` nått, 429 (60 s block `places:block`) eller annat Places-fel → `{ forslag:[], kalla:'ingen' }`** (aldrig ett fel – klienten visar ingen lista och fältet fungerar som fritext). **403/`PERMISSION_DENIED` (även legacy `REQUEST_DENIED`) = Places API (New) inte aktiverat eller inte tillåtet på nyckeln → dessutom `varning:'places'` och CacheService `maps:varning` = "Places API ej aktiverat"** som `ping.mapsVarning` visar i Drift-panelen (en befintlig Geocoding/Distance Matrix-varning "Nyckeln avvisad …" skrivs inte över – den gäller även restiden) samt `places:nekad` i 60 s: under tiden svarar scriptet samma sak utan nytt Places-anrop (annars gjorde varje klients minutförsök ett nekat anrop mot `MAPS_DAILY_CAP`). En fråga som normaliseras till tomt (t.ex. "Sverige", "...") ger `{ forslag:[], kalla:'places' }` när nyckel finns (vanligt "inga träffar" – `'ingen'` får klienterna att pausa förslagen). Texter får `<`/`>` ersatta och klipps till 160 tecken. Cache: CacheService `ac:<sha256(normaliserad q)>` i 6 h (CacheService-max; steg 2b-briefen säger 24 h) – samma normalisering som geokodningens adressnyckel (gemener, utan skiljetecken, ett mellanslag, utan "sverige"), en post < 5 KB | `E_KEY`, `E_ADMIN`, `E_VALIDATION` (`falt.q/sessionToken`), `E_RATE` (typ `adressforslag` – 60/min eller 600/dag per kod nått; klienten döljer listan tyst), `E_SETUP` |

**Places-anropet** (`placesAutocomplete_` i Availability.gs – det enda stället som anropar `places.googleapis.com`, isolerat så att det kan stubbas): `POST https://places.googleapis.com/v1/places:autocomplete` med header `X-Goog-Api-Key: <MAPS_API_KEY>` och body `{ input: q, includedRegionCodes: ['se'], languageCode: 'sv', sessionToken }`; svaret läses ur `suggestions[].placePrediction.{ placeId | place, text.text, structuredFormat.mainText.text, structuredFormat.secondaryText.text }` (fältnamn enligt Googles dokumentation "Autocomplete (New)"; ingen `X-Goog-FieldMask` behövs för autocomplete). Googles feltexter ekas aldrig till klienten och loggas inte.

**Gränser (4.5, steg 2b):** `MAX_ADRESSFORSLAG_PER_KOD_M` 60/min och `MAX_ADRESSFORSLAG_PER_KOD_D` 600/dag per kod (admin räknas som koden `admin`) i egna räknare `ac:m`/`ac:d` – **utanför** de allmänna anropsgränserna (`authBokare` med `anropsgrans:false`), eftersom ett adressfält skickar ett anrop per tangenttryck (debounce 300 ms) och annars skulle äta upp bokarens 30 anrop/min för `availability`/`book`. Varje anrop räknas (även cache-träffar och tomma svar). Varje riktigt Places-anrop räknas som 1 element mot `MAPS_DAILY_CAP` (som en geokodning).

**`placeId` i geokodningen:** när klienten skickar `placeId` bredvid `adress` anropar `geocodeAddress(adress, { placeId })` Geocoding API med `place_id=<id>&language=sv` i stället för `address=…&region=se&components=country:SE` – exakt träff (ingen tolkning av fritexten; samma Geocoding-SKU som adressgeokodning, vinsten är exakthet). Cachekedja: körningens memo → CacheService `geo:pid:<sha256(placeId)>` (6 h) → API. **Resultatet cachas aldrig under den skrivna adressens nyckel** (varken CacheService `geo:<hash>` eller cache-filens `geokod[nyckel]`): adress och `placeId` kommer båda från klienten och scriptet kan inte verifiera att texten hör till id:t – annars kunde en bokare skicka `{ adress:'Kungsgatan 1, Stockholm', placeId:<id för Kiruna> }` och binda den adresstexten till fel koordinater för alla bokare (cache-filen läses för alltid). I stället cachas place-id-resultatet under `geo:pid` och under den **formaterade adressens** nyckel (Googles egen text) i CacheService och cache-filen, plus i körningens memo (`AVAIL_GEO_MEMO`) under den skrivna nyckeln så att `reserve`/`book`/`computeAvailability` (som bara har adressen) blir träffar i samma körning; nästa anrop (`availability` → `reserve` → `book`) bär `placeId` igen och träffar `geo:pid`. Svarar Geocoding `INVALID_REQUEST`/`NOT_FOUND` på ett `place_id` (inaktuellt id – Google säger att place-id:n kan bli inaktuella) sätts ingen "Nyckeln avvisad"-varning; scriptet geokodar i stället adressen som förut i samma anrop (ett element till) och det resultatet cachas som en vanlig adressgeokodning. `placeId` valideras mot `/^[A-Za-z0-9_-]{10,300}$/` (annars `E_VALIDATION falt.placeId`, i `book`/`egen-update` tillsammans med övriga fältfel), sparas **inte** på inkorgsposten och räknas mot samma per-kod-gränser som en vanlig geokodning (`MAX_GEOCODE_PER_KOD_H`/`MAX_ADRESSER_PER_KOD_D`). I `egen-rebook`/`rebook` hör `placeId` till en **ny** `adress` – vid ren tidsflytt (adress utelämnad eller samma som postens) ignoreras det och **postens egen `geo` återanvänds** (primar körningens memo, inget Geocoding-anrop – adresstexten geokodas inte om). Som ankare i andras tillgänglighet bär inkorgsposten sina koordinater; `mergeBusy` (Calendar.gs) föredrar en redan geokodad plats framför kalenderhändelsens rena platstext, så en bokad adress geokodas inte om per text. `sessionToken` skickas aldrig till Geocoding (Geocoding tar ingen sessionstoken); sessionen avslutas genom att klienten slutar skicka token.

**Kostnad (verifiera mot aktuell prislista i Google Maps Platform › Pricing):** enligt Googles dokumentation (Places API (New) › "Session tokens" / "Usage and billing") avslutas en Autocomplete (New)-session **bara** av ett Place Details (New)- eller Address Validation-anrop med samma `sessionToken`; en övergiven session debiteras som om ingen token skickats. Geocoding API tar ingen sessionstoken och avslutar ingen session – alltså faktureras **varje Places-anrop i modulen per förfrågan** (SKU "Autocomplete Requests"; räkna med ~5–10 anrop per adress med 300 ms debounce), och `sessionToken` ger i dagens design ingen rabatt (skickas ändå: korrekt sessionering om Place Details införs senare). Kostnadsskyddet är cachen per normaliserad fråga (6 h), kodgränserna 60/min och 600/dag, `places:nekad`/`places:block` och `MAPS_DAILY_CAP`. Geokodning med `place_id` kostar samma Geocoding-SKU som adressgeokodning – vinsten är exakt träff, inte pris. Googles månatliga fribelopp täcker normal användning av modulen. *Möjligt senare steg (designändring, inte i steg 2b):* geokoda valt förslag med Place Details (New) `GET https://places.googleapis.com/v1/places/<placeId>` med `X-Goog-FieldMask: location,formattedAddress` och samma `sessionToken` i stället för Geocoding – då avslutas sessionen och autocomplete-anropen blir sessionsprissatta; verifiera prislistan vid deploy.

## Admin-endpoints (M3/M4/M5, spec 4.4) – anropas av appen med `adminKey`

Alla admin-anrop går genom `bokAdminApi(action, payload)` i `index.html` (adminnyckeln läggs på automatiskt), ~60 anrop/min, konstanttidsjämförelse. Svar i det vanliga kuvertet (`ok`, `data`, `scriptVersion`, `configRev`, `serverTime`).

| Action | Request (utöver `adminKey`) | `data` vid `ok:true` | Fel |
|---|---|---|---|
| `setup` | `{ fileIds:{ config, inbox, cache } }` (även `configFileId`/`inboxFileId`/`cacheFileId`) | `{ version, scriptVersion, konfigurerad:true, ownerEmail, kalendrar:[…] }` | `E_SETUP` "Adminnyckel saknas i Script Properties" (ingen `details.fil`) när `ADMIN_KEY` inte lagts in, `E_ADMIN` när nyckeln avviker, `E_VALIDATION` (`falt.config/inbox/cache/fileIds`), `E_SETUP` med `details.fil` (`'config'|'inbox'|'cache'`) |
| `config-push` | `{ rev }` | `{ ok:true, rev, configRev, varningar:[] }` – cachen töms och filen läses färskt | `E_STATE` med `details.configRev` när filens `rev` < begärd (spec 4.3/4.4; appen försöker om efter 2 s, max 3 ggr); nyare fil är ok |
| `calendars-list` | – | `{ kalendrar:[{ id, summary, namn, primary, primar, accessRole }] }` (primär först, sedan namn) | `E_CALENDAR` |
| `inbox-list` | `{ status?: 'ny' \| ['ny','importerad','avvisad','avbokad'], limit?: 1–500 }` | `{ bokningar:[…nyaste först], antal, totalt, rev }` (default 200) | `E_VALIDATION`, `E_SETUP` |
| `ack` | `{ enhetId, bokningIds:[…] }` (även `{ deviceId, bokningar:[{ bokningId, leadId, eventId, kundId, kontaktId, nyProcess, nyKund, mojligDubblett }] }` – planen sparas på posten) | `{ resultat:[{ bokningId, claimed, status, importeradAv, importeradTs, saknad?, plan? }], claimed:[id], alreadyClaimed:[{ bokningId, importeradAv, importeradTs, plan }], missing:[id] }` – under lås, `ny → importerad`, en inkorgsskrivning | `E_VALIDATION`, `E_LOCK` |
| `reject` | `{ bokningId, orsak? }` (orsak ≤ 500 tecken) | `{ ok:true, bokningId, status:'avvisad', kalenderBorttagen, kalenderFel, mejlSkickat, bokning }` – från `ny`/`importerad`; `Calendar.Events.remove` (`sendUpdates:'all'`, 404/410 = redan borta = ok; annat API-fel → status sätts ändå + historik `kalenderfel` + `kalenderFel:true` i svaret, enligt spec 4.7 – appen ska visa varningen; `mejlSkickat:false` när bokaren saknar giltig e-post); plain text-mejl till `bokare.epost` | `E_NOT_FOUND`, `E_STATE` (`details.status`), `E_VALIDATION`, `E_LOCK`, `E_SETUP` |
| `calendar-preview` | `{ from, to }` (`YYYY-MM-DD`, inklusive; även `{ fran, till }`), högst 8 veckor (56 dagar), inom `[idag−7, horisont+7]` | `{ from, to, idag, horisontTom, genererad, handelser:[…], resor:[…], icsStatus:{ ok, hamtadTs, antal, medPlats, preliminara, hamtningMs, cachad, langsam }, obesvarade, varningar:[] }` – exakt det scriptet ser efter sammanslagning, ALLA källor (Google `tider`/`fullt`, Outlook-ICS, inkorgens bokningar, aktiva reservationer); ignorerade poster är MED (`ignorerad:true`), preliminära ICS-poster är med (`preliminar:true`, `raknad` enligt config); en händelse över midnatt ger ett segment per dag. Post: `{ id, ignoreraId, serieId, matchIds, kalla:'bokningar'\|'privat'\|'ics'\|'reservation', datum, start, slut, heldag, titel, plats, omrade, overrideOnline, overrideAdress, hasPlace, restid, cooldownMin, preliminar, raknad, ignorerad, bokningId, bokareId, motestypId, sammanslagenMed:[], varning, varningar:[] }` – `titel`/`plats` bara för läge `fullt`, ICS och reservationer (`'Reservation'`), **aldrig** för läge `tider` (`kalla:'privat'` → alltid `''`; `hasPlace`/`restid` bara boolska); `omrade` (steg 2c) = områdesetikett `'Stad · Stadsdel'` (≤ 60 tecken) för varje händelse med geokodad plats, även `privat` (bara ortnamn – aldrig adressen), annars `''`; `overrideOnline`/`overrideAdress` (version 9, valfria) = händelsens manuella restidsinställning ur `KALENDER_IGNORERA` (`true\|false\|null` resp. rättad adress eller `''`, se "Version 9"); `serieId` (version 9, valfri) = Googles `recurringEventId` för en instans av en återkommande händelse, annars `''` – en `KALENDER_IGNORERA`-post med `id = serieId` gäller hela serien, med `id = ignoreraId` bara instansen (Google) resp. hela serien (ICS UID). **`resor` (steg 2c, A57; version 10 `delar`):** `[{ datum, franId, tillId, start:'HH:MM', slut:'HH:MM', minuter, status:'ok'\|'schablon'\|'okand', konflikt:boolean, delar:[{ start, slut }] }]` – restid mellan på varandra följande restidsankare samma dag (`restid:true`, ej ignorerade/heldag; Teams/platslösa händelser hoppas över utan att bryta kedjan men är hinder för placeringen) plus `franId:'bas'` → dagens första ankare när `restidTillForsta` och sista ankare → `tillId:'bas'` när `restidEfterSista` (basen ogeokodad → bas-benet får schablonminuter + `schablon`, samma tid som `dayPlan` reserverar för bokarna; ett segment som börjar 00:00 eller slutar 24:00 – händelse över midnatt – får inget bas-ben vid dygnsgränsen). `minuter` = Distance Matrix **råa minuter** (version 10, inget påslag) → `ok`; "för långt" (fågelväg > 150 km) → uppskattning fågelväg/70 km/h i råa minuter, också `ok` (ingen Distance Matrix); två ankare på samma koordinater → 0 s utan anrop och benet **utelämnas** ur `resor` (version 7, K3 – tidigare 0 s + marginal); ogeokodad ändpunkt → schablonminuter → `schablon`; par som inte kunde beräknas nu → schablonminuter + `okand`. Placering (version 10, `placeTravelDelar`): inresan fyller de fria hålen närmast mötets start och **delas runt platslösa hinder** (inkl. deras effektiva buffert), utresan efter sista mötet börjar vid mötets slut + effektiv buffert (`cooldownMin`) och delas framåt; `delar:[{ start, slut }]` (valfritt, kronologiskt) är de faktiska bitarna appen ritar, `start`/`slut` det yttre spannet; ryms inte hela restiden mellan föregående mötes slut + buffert och mötets start → `konflikt:true` och `delar` = ett block närmast mötet. `cooldownMin` per händelse = den effektiva bufferten (max(cooldown, marginal fysiskt/online), version 10). Ankare med bara platstext geokodas i anropet (samma cachekedja som `availability`; Geocoding-element mot `MAPS_DAILY_CAP`). **Kostnadstak:** högst `AVAIL_PREVIEW_MAX_PAR` = 60 nya (ocachade) par per anrop – resten `okand` + varningen "Restid för fler par än taket beräknas nästa gång"; och högst `AVAIL_PREVIEW_MAX_GEO` = 60 nya Geocoding-anrop per anrop (fönstret är upp till 56 dagar, varje ocachad platstext är ett synkront UrlFetch-anrop) – resten förblir ogeokodade (`omrade:''`, schablon i `resor`) + varningen "Fler platser än taket geokodas nästa gång"; cache-träffar (CacheService 6 h + cache-filens `geokod`/`restid`, inkl. `ZERO_RESULTS`-poster) kostar inget, så upprepade anrop är gratis. inga kundnamn ur inkorgen (matcha på `bokningId`); `id` = intern busy-id (används i `sammanslagenMed`), `ignoreraId` = det id `KALENDER_IGNORERA` ska lagra (Google event-id / ICS UID / bokningens kalenderhändelse-id), `matchIds` = alla id:n posten matchas på. `varningar`: ICS-fel ("Outlook-flödet kunde inte läsas kl HH:MM"), "Många obesvarade Outlook-inbjudningar (N)", "Maps: …", "Kalenderhändelser som saknas i inkorgen: N (M i vyn)" (4.11, A52: den nattliga listan – Script Property `avstamning_saknas` + CacheService `avstamning:saknas`, fylls av `dailyMaintenance` – **plus en live-jämförelse av vyn mot inkorgen**: varje `kalla:'bokningar'`-post med `bokningId` i giltigt format utan inkorgspost räknas direkt, även före första nattkörningen och när triggern dött; anonymiserade händelser `Möte (borttaget)` räknas inte; berörda poster får `varning:'saknas i inkorgen'`), "Outlook-flödet svarade långsamt kl HH:MM – kopian från kl HH:MM används" (A46). "N avbokade/avvisade möten ligger kvar i kalendern – ta bort dem manuellt" (4.7: händelse vars `bokningId` är `avbokad`/`avvisad` i inkorgen, t.ex. efter misslyckad `Calendar.Events.remove`; posten får `varning:'Avbokad i inkorgen men händelsen finns kvar i kalendern – ta bort den manuellt'` resp. `Avvisad …`). `obesvarade` = antal preliminära i fönstret | `E_VALIDATION` (`falt.from/to`), `E_CALENDAR`, `E_SETUP` |
| `rebook` | `{ bokningId, start, adress?, reservationId?, motestypId?, orsak? }` – `start` = ISO med offset eller `{ datum, tid }`, samma regler som `book` (raster, öppen dag, framförhållning per mötestyp, horisont); `adress` = ny adress (typ med restid; utelämnad → befintlig); `motestypId` = bokningens egen (default) eller en aktiv typ bokaren får boka; `reservationId` undantas bara om reservationen tillhör bokningens bokare eller en CJ-bokare | `{ bokningId, start, slut, adress, geo, restid, motestypId, kalenderEventId, kalenderNyHandelse, rev, mejlSkickat, bokning }` – från `ny`/`importerad`; bokningens mötestyp tillåts även om den är inaktiv/inte längre i `tillatnaMotestypIds`; nätverks-I/O (ICS, geokodning, Distance Matrix) före låset; under lås: färsk kalenderläsning med `computeAvailability(undantaBokningId = bokningId)` → `E_SLOT_TAKEN`, `Calendar.Events.patch(start/end/location[/summary], sendUpdates:'all')` (händelsen borta, 404/410 → ny händelse skapas: `kalenderNyHandelse:true`, nytt id; annat fel → `E_CALENDAR`, inget ändrat), posten `start/slut/adress/geo/restid`, `rev+1`, `andradAt`, historik `{ typ:'ombokad', av:'cj', fran, till[, orsak, motestypFran] }` – **status oförändrad** (appen speglar via `rev > bokningRev`, 8.3); misslyckas inkorgsskrivningen flyttas händelsen tillbaka (best effort) → `E_INTERNAL`; busy-cachen töms för gamla och nya dagen; plain text-mejl till `bokare.epost` med gammal och ny tid | `E_NOT_FOUND`, `E_STATE` (`details.status`), `E_SLOT_TAKEN`, `E_VALIDATION` (`falt.bokningId/start/adress/orsak/motestypId`), `E_CALENDAR`, `E_LOCK`, `E_SETUP` |
| `cancel` | `{ bokningId, orsak? }` (orsak ≤ 500 tecken; även `{ kalenderEventId, orsak? }` utan `bokningId` – spec 4.4: avbokning efter gallring; finns ingen inkorgspost med det händelse-id:t tas bara kalenderhändelsen bort, `postSaknas:true` – då görs först `Calendar.Events.get` så att `busy:<datum>` töms och händelsens `bokningId` stryks ur avstämningslistan) | `{ ok:true, bokningId, status:'avbokad', kalenderBorttagen, kalenderFel, mejlSkickat, rev, postSaknas, bokning }` – från `ny`/`importerad`; under lås: `Calendar.Events.remove` (`sendUpdates:'all'`, 404/410 = redan borta = ok; annat fel → status sätts ändå + historik `kalenderfel` + `kalenderFel:true`, 4.7), status `avbokad`, `avbokadTs`/`avbokadOrsak`/`andradAt`, historik `{ typ:'avbokad', av:'cj', orsak }`, inkorgen skrivs (`rev+1`), busy-cachen töms – posten raderas aldrig (8.3); plain text-mejl till `bokare.epost` | `E_NOT_FOUND`, `E_STATE` (`details.status`, t.ex. redan `avbokad`/`avvisad`), `E_VALIDATION`, `E_LOCK`, `E_SETUP` |
| `purge` | `{ bokningIds?:[…], kalenderEventIds?:[…], orgnr?, adresser?:[…] }` – minst ett fält med innehåll; högst 100 id:n per lista och 20 adresser (`PURGE_MAX_ADRESSER`); `bokningIds` 36 tecken `[0-9a-f-]`, `kalenderEventIds` = appens `ev.kalenderEventId` (`[A-Za-z0-9_@.-]{1,256}`), `orgnr` normaliseras med `normalizeOrgnr` (ogiltigt → `E_VALIDATION falt.orgnr`; tomt = ingen orgnr-matchning), `adresser` ≤ 200 tecken | `{ borttagna, kalenderRaderade, anonymiserade, geokodBorttagna, kalenderFel:[{ eventId, bokningId, typ:'radera'\|'anonymisera'\|'sok'\|'hamta' }], rev }` – under lås (4.8): inkorgsposter vars `bokningId` ingår **eller** vars `kund.orgnr` = `orgnr` tas bort (alla statusar; `borttagna` = antal, `rev` = inkorgens rev efter skrivningen); kalenderhändelser hittas via posten (`kalenderEventId`), via `kalenderEventIds` (`Calendar.Events.get` avgör passerad/framtida; 404/410 = redan borta, ignoreras) och via `Calendar.Events.list(calId, { privateExtendedProperty:'bokningId=<id>' })` för bokningId utan känt event-id (samma händelse från flera håll slås ihop och sökträffen återanvänds – ingen extra `get`; redan anonymiserade träffar filtreras bort); **framtida** händelse (slut > nu) → `Calendar.Events.remove(sendUpdates:'all')` (`kalenderRaderade`; 404/410 = ok, räknas inte), **passerad** → först `Calendar.Events.get` (borta 404/410, `status:'cancelled'` – t.ex. avbokad/avvisad post vars händelse cancel/reject redan tagit bort – eller redan `Möte (borttaget)` → hoppas över utan patch och utan notis), annars `Calendar.Events.patch({ summary:'Möte (borttaget)', location:'', description:'', extendedProperties.private.{bokningId,bokareId,pipelineId,motestypId}: null }, sendUpdates:'all')` så Outlook-kopian uppdateras (`anonymiserade`; A19 – aldrig radering bakåt i tiden, gäster orörda); annat kalenderfel fäller inte anropet utan hamnar i `kalenderFel` (appen visar varning); utan vald bokningskalender → `kalenderFel typ 'radera'` för varje känd händelse; geokodposter i cache-filen för posternas adresser + `adresser[]` tas bort (`geokodBorttagna`), liksom CacheService `geo:<hash>`, `busy:<datum>` för berörda dagar (hela preview-fönstret `[idag−7, horisont+7]` när någon kalenderhändelse hanterats – händelser utan post har ingen känd dag) och id:na i avstämningslistan (`avstamningStryk`, `ts` orörd). Inga mejl, ingen nätverks-I/O. | `E_VALIDATION` (`falt.bokningIds` "Inget att radera" / "Ogiltigt värde", `falt.kalenderEventIds`, `falt.orgnr`, `falt.adresser`), `E_ADMIN`, `E_LOCK`, `E_SETUP` |
| `block-sync` | – | `{ ok, aktiv, in, bort, andrade, kvar, antal, kalenderId, fel, hoppad, upptagen, kalenderNamn:'Pipeline – restid' }` (version 11, "Synka nu") – `syncBlock_({ maxSkriv: 80 })`: avstämning av restid-/marginalblocken i kalendern "Pipeline – restid" mot den aktuella planen `[idag, horisontTom]` (skapar kalendern första gången); `kvar` > 0 = taket/tidsbudgeten nåddes, klicka igen; `ok:false` med `fel` > 0 = Calendar-fel (körningen fortsätter ändå, max 5 i följd); `ok:false` med `hoppad` = `'inkorg'`/`'outlook'` = källan var ofullständig, inga skrivningar (försök igen om en stund); `upptagen:true` = triggerns körning pågår, inga skrivningar; `aktiv:false` = synken är av (då städas fönstret om ett kalender-id finns). Se "Version 11". | `E_ADMIN`, `E_SETUP` |

## `ping` (oautentiserat, 4.4, 10.4)

`{ scriptVersion, konfigurerad, orsak, mapsNyckel, mapsForbrukningIdag, mapsVarning, mapsDagstak, icsStatus:{ ok, hamtadTs, antal, medPlats, preliminara, hamtningMs, cachad, langsam }, bokningarIdag, underhallSenast, avstamningSaknas }` – M5 lade till `mapsDagstak` (Script Property `MAPS_DAILY_CAP`, default 1000), `icsStatus.hamtningMs` (senaste Outlook-hämtningens tid i ms), `icsStatus.cachad` (resultatet ryms i CacheService), `icsStatus.langsam` (senaste hämtningen > 5 s → reservkopia används vid cache-miss i 30 min; flaggan är `true` bara under de 30 minuterna, samma villkor som reservvägen), `bokningarIdag` (Script Property `book_count_<idag>`), `underhallSenast` (ISO för senaste lyckade `dailyMaintenance`, '' innan första körningen) och `avstamningSaknas` (antal kalenderhändelser som saknas i inkorgen). Inställningar › Drift och kvoter visar dessa som sanity checks (10.4, A50; saknas fälten – äldre script – visas "–"); `calendar-preview.icsStatus` har samma fält. Version 11 lade till `blockSynk: { aktiv, kalenderId, senast:{ ts, in, bort, andrade, kvar, antal, fel } | null }` (blocksynkens status – Inställningar › Kalendrar visar "Senast synkad HH:MM · N block · M väntar"). Steg 2b lade inte till några fält: Places-status syns i `mapsVarning` ("Places API ej aktiverat" när Places API (New) saknas – Drift-panelen visar den som Places-status; en Geocoding/Distance Matrix-varning "Nyckeln avvisad …" har företräde och skrivs inte över av Places) och Places-anropen i `mapsForbrukningIdag`.

### Appens användning av `purge` (8.5, 9, A49)

`leadDeleteBtn`/`customerDeleteBtn` i `index.html` markerar först `BOKNING_IMPORT`-posterna `raderad`, anropar sedan `purge` och raderar lokalt bara när svaret är `ok:true`. Appen skickar enbart id:n med scriptets format (`bokningId` 36 tecken `[0-9a-f-]`, `kalenderEventId` `[A-Za-z0-9_@.-]{1,256}`) och bara ett giltigt, normaliserat orgnr (ogiltigt orgnr utelämnas – det kan inte matcha och skulle ge `E_VALIDATION`); processradering skickar händelsernas mötesadresser i `adresser` (geokodposten tas bort även när inkorgsposten redan är gallrad), kundradering dessutom importposternas `bokningId` för kunden samt kundens adress. Kalenderkolls "Ta bort ur kalendern" (föräldralös händelse med `varning:'saknas i inkorgen'`) anropar också `purge` (`{ bokningIds:[bokningId], kalenderEventIds:[eventId] }`) – inte `cancel` – så att en kommande händelse raderas, en passerad anonymiseras (A19) och id:t stryks ur avstämningslistan direkt; "Kontrollera avstämning" i Drift-panelen är `calendar-preview` idag−7 … horisont+7 (högst 56 dagar). Svarets `kalenderFel[].typ` visas som "kunde inte tas bort / anonymiseras / sökas i kalendern / hämtas" per händelse-id med statusraden "kalenderhändelser måste tas bort manuellt" + "Visa kalendern". Varje `!ok` (även `E_NETWORK`/`E_ABORT` efter 45 s, `E_LOCK`, `E_NOT_IMPLEMENTED` "– uppdatera scriptet") avbryter raderingen och återställer markeringen; är modulen inte ansluten raderas lokalt med varning om manuell borttagning. Harness: `contract-m5.js` kör appens knapp-handlers mot `handlePurge` i samma Node-process.

## `dailyMaintenance` (4.11, trigger kl 03–04, M5)

En körning per dygn, en loggrad `{ trigger:'dailyMaintenance', ok, ms, raknare, inkorg, utanImport, geokod, restid, omrade, saknas, fel:[] }` (Körningar/Cloud Logging, inga personuppgifter). Steg:

1. **Räknare:** `maps_elements_*`/`book_count_*`/`andr_count_*` äldre än 7 dagar tas bort ur Script Properties (körs även om brevlådan inte är ansluten).
2. **Inkorg** (under lås, 20 s): poster med status `importerad`/`avvisad`/`avbokad` tas bort när referenstiden (senaste av `andradAt`/`importeradAt`/`avvisadTs`/`avbokadTs`, reserv `skapad`) är äldre än 30 dagar **och** `slut` har passerat (framtida möten behålls – rebook/cancel/egna bokningar ska fungera); `ny`-poster tas bort när `slut` passerat med > 90 dagar (`utanImport` i loggraden = "gallrad utan import"). Okänd status eller otolkbara datum → posten behålls. Inkorgen skrivs en gång (`rev+1`) bara om något togs bort.
3. **Cache-fil** (samma lås): `geokod`-/`restid`-poster äldre än 180 dagar (`ts`) tas bort; geokodposter vars adress hör till en post som gallrades i steg 2 markeras `gallrad:true` och tas bort när de är äldre än 90 dagar (`purge` tar bort dem direkt). `icsReserv` rörs inte.
3b. **Backfill av `omrade`** (steg 2c, utanför låset – cache-filen är en ren cache): högst 50 geokodposter per natt som saknar `omrade` geokodas om (`backfillOmrade_`, Geocoding-element mot `MAPS_DAILY_CAP`); gallrade poster (`gallrad:true`) och `status:'okand'`-poster hoppas över; nyckel/kvot-fel avbryter natten, ett enstaka annat fel hoppar över posten (max 3 i rad). `omrade` i loggraden = antal. Engångsjobb – när alla poster har fältet gör steget inget.
4. **Avstämning kalender ↔ inkorg (A52):** `Calendar.Events.list(bokningskalendern, { timeMin: nu−7 d, timeMax: horisont+7 d, singleEvents, showDeleted:false, fields:… })` (`listGoogleEventsAvstamning_` – alltid tidsavgränsad; `updatedMin` utan tidsgräns skulle expandera ett aldrig slutande återkommande möte i en delad kalender till 50 × 2 500 poster) – händelser med `extendedProperties.private.bokningId` i giltigt format som saknar inkorgspost (och inte gallrades i samma körning): **alla framtida** (en framtida händelse utan post är alltid föräldralös – framtida poster gallras aldrig, purge tar bort båda – och blockerar luckan tills den tas bort; den ligger därför kvar i listan natt efter natt) samt **passerade de senaste 7 dagarna som ändrats/skapats de senaste 7 dagarna** (`updated`; äldre gallrade möten med en gästuppdatering flaggas inte). Anonymiserade händelser `Möte (borttaget)` hoppas över (V13). Hittas något läses inkorgen om (en Drive-läsning) och id:n som hunnit få post sedan låset släpptes stryks (annars falsk varning i 36 h för en bokning som skapades under körningen). Listan skrivs till Script Property `avstamning_saknas` `{ ts, ids }` (giltig 36 h) och CacheService `avstamning:saknas` (6 h). `calendar-preview` visar "Kalenderhändelser som saknas i inkorgen: N" + `varning:'saknas i inkorgen'` per post, `ping.avstamningSaknas` antalet. Kalenderhändelser rörs aldrig av gallringen. Ingen bokningskalender vald → steget hoppas över.
5. `maintenance_senast` sätts bara när alla steg lyckades; annars `ok:false` och `fel` (t.ex. `inkorg:E_LOCK`, `avstamning:E_CALENDAR`) – de övriga stegen körs ändå.

Körtid: en Drive-läsning (två av inkorgen när avstämningen hittar något) + högst en skrivning per fil och en avgränsad `Events.list` (≈ 2 månader i Bokningar-kalendern); 3 000 inkorgsposter + 2 000 geokodposter tar ~30 ms i Node (I/O tillkommer i Apps Script: ~5–10 s), långt under 6 min. Okonfigurerat script (`E_SETUP`) → bara steg 1, `ok:true`.

## ICS-cache (4.6, M5 – prestanda för stora Outlook-flöden)

- **Filtrering före allt:** det parsade flödet reduceras till `{ uid, heldag, start, slut, plats (≤ 200), summary (≤ 160), status, preliminar, varning }` och filtreras till `[idag−1, horisont+1]` (även enstaka händelser – tidigare filtrerades bara serier) innan det cachas, skrivs till `icsReserv` eller används.
- **Komprimerad, chunkad CacheService-cache** (15 min): JSON → `Utilities.gzip` → base64 → bitar om högst 90 KB under `ics:busy:0 … ics:busy:<n−1>`; indexnyckeln `ics:busy` = `{ v:2, delar, langd, hamtadTs, antal }`. Läsning = `getAll` på bitarna + kontroll av antal/längd + `Utilities.ungzip`; saknad bit/fel längd/okänd version = cache-miss (hämta om). Fler än 8 bitar (`KAL_ICS_MAX_CHUNKS`, ≈ 540 KB gzip) → ingen cache, parsa varje gång (memot per körning skyddar fortfarande låset). CJ:s flöde (~370 händelser, > 90 KB JSON) blir ~5–10 KB base64 → en bit. Det äldre formatet (ren JSON-lista under `ics:busy`) läses fortfarande.
- **Andra anropet inom 15 min** gör alltså varken `UrlFetchApp` eller parsning; kvar är `Calendar.Events.list` per kalender, Drive-läsning av inkorgen och beräkningen. `icsStatus.cachad` visar att cachen togs.
- **Långsamt Outlook (A46):** tar hämtningen > 5 s (`icsStatus.hamtningMs`, `langsam:true`) används i 30 min därefter cache-filens `icsReserv` (Drive ~1 s) vid cache-miss i stället för ny hämtning – om reserven är yngre än 60 min; resultatet cachas i CacheService och varningen "Outlook-flödet svarade långsamt kl HH:MM – kopian från kl HH:MM används" visas i Kalenderkoll. Efter 30 min görs en ny riktig hämtning (som förnyar reserven). Misslyckad hämtning → som förut: reserv + "Outlook-flödet kunde inte läsas kl HH:MM" och 5 min paus.
- **Tidszon utan Java-brygga:** `tzParts`/`toIsoWithOffset` räknar Stockholms offset i ren JS (EU-regeln) efter en självkontroll mot `Utilities.formatDate` med sex instanter (vinter, sommar, båda omställningarna); misslyckas kontrollen används bryggan som förut. Sparar ~1 ms per unik tidpunkt – ett flöde med hundratals händelser och gamla serier gav tidigare tusentals bryggeanrop. RRULE-expansionen snabbspolar dessutom serier utan COUNT till strax före fönstret och hoppar över tidszonsarbete för instanser som ligger före det (identiskt resultat, verifierat mot koden före M5; bieffekt: dagliga serier startade > 1 000 dagar bakåt, som tidigare försvann vid skyddsgränsen, räknas nu).
- **Cache-filen läses en gång per anrop (A51):** nästa flaskhals efter ICS-cachen var geokodningen av ICS-platserna – varje CacheService-miss (`geo:<hash>`, TTL 6 h, alla nycklar går ut ungefär samtidigt) gjorde en egen Drive-läsning av cache-filen (~0,3–1 s), så första `availability` efter varje 6 h-fönster tog tiotals sekunder med CJ:s ~100 platser (bokningssidans timeout 15 s). Nu memoiseras filen per körning (`AVAIL_FIL_MEMO`, nollställs i `doPost`) och nya geokodposter skrivs samlat en gång per körning. Uppmätt i harnessen (m5-suite rev-1, 28 unika ICS-platser + bokarens adress): kall CacheService med varm cache-fil → **1** Drive-läsning (tidigare 30), första anropet med alla adresser nya → 4 läsningar + 3 skrivningar (tidigare 61 läsningar och en skrivning per adress). Geocoding-API-anropen är oförändrade (en gång per ny adress, sedan 6 h CacheService + cache-filen).
- **Mätning i Node** (m5-suite.js, 301 VEVENT = 300 enstaka + en WEEKLY-serie med COUNT=100 → 307 instanser i fönstret): parse + instanser ≈ 20–30 ms, cache-skrivning (gzip+base64) ≈ 5 ms, cache-läsning ≈ 1–4 ms, 70 KB JSON → 5,7 KB base64, `Utilities.formatDate`-anrop 8 (mot 399 utan snabbvägen). I Apps Script tillkommer tjänsteanropen (UrlFetch mot Outlook är den stora posten vid cache-miss).

## Sanity checks i drift (10.4)

| Kontroll | Var | Förväntat |
|---|---|---|
| Scriptversion | `ping.scriptVersion` | ≥ `MIN_SCRIPT_VERSION` i appen/sajten |
| Brevlådan ansluten | `ping.konfigurerad`, `orsak` | `true`, `''` |
| Trigger lever (V1) | `ping.underhallSenast` | ISO-tid < 30 h gammal (kl 03–04 varje dygn); saknas den efter 2 dygn: kör `install()` om |
| ICS-värmare (version 7) | `ping.icsStatus.hamtadTs`, Körningar | `hamtadTs` ≤ ~10 min gammal även utan anrop från klienterna; loggrader `{ trigger:'refreshIcsCache', … }` var 10:e minut – saknas de: kör `install()` om |
| Maps | `ping.mapsNyckel`, `mapsForbrukningIdag`, `mapsVarning` | nyckel finns, förbrukning < `MAPS_DAILY_CAP`, tom varning ("Places API ej aktiverat" = Places API (New) saknas på nyckeln/projektet – adressförslag avstängda, restid opåverkad) |
| Outlook-ICS | `ping.icsStatus` | `ok:true`, `hamtadTs` färsk (≤ 15 min efter ett anrop), `cachad:true`, `langsam:false`, `hamtningMs` < 5 000; `preliminara` = obesvarade inbjudningar |
| Bokningar i dag | `ping.bokningarIdag` | < `MAX_BOOK_GLOBAL_D` (40) |
| Avstämning | `ping.avstamningSaknas` (nattlig lista), "Kontrollera avstämning"/Kalenderkoll (`calendar-preview` = nattlig lista + live) | 0; annars visar Kalenderkoll vilka händelser som saknar inkorgspost ("Ta bort ur kalendern" → `purge`, eller boka om). Live-kontrollen ser en föräldralös händelse direkt – även innan triggern kört (V1) |
| Scriptversion vs purge | Drift-panelen (`PURGE_SCRIPT_VERSION = 3` i appen) | ingen varning "purge saknas"; annars är M5-versionen inte distribuerad (Hantera distributioner › Ny version) |
| Förberäknad kalender (version 12) | Körningar, `calendar-preview.tider` | loggrader `{ trigger:'previewSnap', ok:true, ms, dagar, handelser, resor, las }` var 10:e minut (mellan `refreshIcsCache`- och ev. `syncBlock`-raden), `ms` normalt några sekunder – `previewSnap.ms` + `syncBlock.ms` återkommande > 30 000 = kvotrisk (144 körningar/dygn mot 90 min trigger-runtime: triggers stängs av resten av dygnet), `ok:false, skal:'invaliderad'` då och då är normalt (en bokning/ändring landade mitt i beräkningen; nästa körning skriver om); `calendar-preview`-raderna har `tider.kalla:'snapshot'` i normalfallet (`'live'` bara efter "Uppdatera", direkt efter en bokning/ändring eller när triggern inte kört på 11 min – då är triggern död: kör `install()` om); appens Drift-panel visar "Kalender: senaste hämtning X,X s (script Y,Y s: …)" |
| Blocksynk (version 11) | `ping.blockSynk`, Körningar | med synken på: `senast.ts` ≤ ~10 min gammal, `senast.kvar` = 0 efter första fyllningen, `senast.fel` = 0; loggrader `{ trigger:'syncBlock', … }` direkt efter varje `refreshIcsCache`-rad. Kalendern "Pipeline – restid" syns i Google Kalender men **aldrig** i Kalenderkoll |
| Loggrader | Körningar | `{ action, bokareId, ok, code, ms }` per anrop (`calendar-preview`/`availability` dessutom `tider`, version 12), `{ trigger:'dailyMaintenance', … }` per dygn, `{ trigger:'refreshIcsCache', ok, ms, kalla }` var 10:e minut (+ `{ trigger:'syncBlock', ok, ms, in, bort, andrade, kvar, antal, fel }` när blocksynken rört kalendern) – aldrig kund-/kontaktfält |

## Manuell reservrutin (bara om Anslut-guiden inte kan köras)

Anslut-guiden i appen gör allt nedan automatiskt via `setup`. Rutinen finns kvar som reserv för test direkt mot scriptet:

1. **Drive-filer.** Skapa tre filer i "Min enhet" med exakt dessa namn och MIME-typ `application/json` (enklast via Google Drive API Explorer, eller ladda upp `.json`-filer – kontrollera att Drive visar dem som JSON och inte som "Text"). Minimalt innehåll enligt spec 4.1:
   - `telexia-bokning-config.json`:
     ```json
     { "schemaVersion": 1, "rev": 1, "updatedAt": "2026-09-15T09:00:00+02:00", "updatedBy": "app",
       "bokare": [ { "id": "bokare_test", "namn": "Test Testsson", "organisation": "TEST", "epost": "",
                     "pipelineId": "p_test", "tillatnaMotestypIds": [], "aktiv": true, "arCj": false,
                     "kodHash": "<sha256 hex av testkoden>" } ],
       "motestyper": [ { "id": "mt_test_fysiskt", "titel": "Testmöte", "langdMin": 60, "cooldownMin": 30, "restid": true,
                         "farg": "", "sannolikhet": 50, "pipelineId": "p_test", "global": false,
                         "bekraftelseMall": "Hej {kontakt}!\nVi ses {datum} kl {tid}–{slut} på {adress}.\n/{bokare}", "aktiv": true },
                       { "id": "mt_test_teams", "titel": "Test Teams", "langdMin": 60, "cooldownMin": 0, "restid": false,
                         "farg": "", "sannolikhet": 50, "pipelineId": "p_test", "global": false,
                         "bekraftelseMall": "Hej {kontakt}!\nVi ses {datum} kl {tid}–{slut}.\n/{bokare}", "aktiv": true } ],
       "formular": { "version": 1, "karna": { "kundnamn": { "synlig": true, "obligatorisk": true }, "orgnr": { "synlig": true, "obligatorisk": true },
                     "adress": { "synlig": true, "obligatorisk": false }, "kontaktperson": { "synlig": true, "obligatorisk": true },
                     "telefon": { "synlig": true, "obligatorisk": true }, "epost": { "synlig": true, "obligatorisk": false },
                     "notering": { "synlig": true, "obligatorisk": false } }, "extrafalt": [] },
       "installningar": { "version": 1, "kalendrar": [ { "id": "<kalender-id för testkalendern>", "namn": "Bokningar TEST", "lage": "fullt" } ],
                          "telexiaEpost": "", "notisEpost": "<CJ:s e-post>", "basadress": "", "basLat": null, "basLng": null },
       "ignorerade": [],
       "pipelines": [ { "id": "p_test", "name": "TEST", "color": "#3b82f6" } ] }
     ```
     Fält som saknas i `installningar` fylls av scriptet med samma defaults som appen (`DEFAULT_BOKNINGSINSTALLNINGAR`).
     `ignorerade` = appens `KALENDER_IGNORERA` (spec 3.5): `[{ id, kalla, lage:'ignorera'|'rakna'|'restid', online?, adress?, lat?, lng?, omrade?, ts }]` – de valfria fälten (version 9) saneras av `normalizeConfig`, se "Version 9".
   - `telexia-bokning-inbox.json`: `{ "schemaVersion": 1, "rev": 0, "updatedAt": "", "updatedBy": "script", "bokningar": [] }`
   - `telexia-bokning-cache.json`: `{ "schemaVersion": 1, "rev": 0, "updatedAt": "", "updatedBy": "script", "geokod": {}, "restid": {}, "icsReserv": null }`
2. **Testbokare.** Välj en testkod på exakt 24 tecken ur `[A-Za-z0-9_-]` (t.ex. `TESTkod_0123456789abcdEF`). Räkna SHA-256 (hex, gemener) av koden – i webbläsarkonsolen: `crypto.subtle.digest('SHA-256', new TextEncoder().encode('TESTkod_0123456789abcdEF')).then(b => console.log([...new Uint8Array(b)].map(x => x.toString(16).padStart(2,'0')).join('')))` – och lägg in värdet som `kodHash`. Klartextkoden ska aldrig in i config-filen.
3. **Testkalender.** Skapa kalendern "Bokningar TEST" i Google Kalender (tidszon Stockholm), kopiera dess kalender-id (Inställningar › Integrera kalender) och lägg in det som `id` i `installningar.kalendrar` med `lage:"fullt"`. Sätt `telexiaEpost` tomt under test (ingen inbjudan skickas) eller till en egen testadress.
4. **Script Properties.** `CONFIG_FILE_ID`, `INBOX_FILE_ID`, `CACHE_FILE_ID` = filernas id (ur Drive-URL:en), `ADMIN_KEY` = en teststräng (t.ex. 32+ tecken `[A-Za-z0-9_-]`). Ev. `MAPS_API_KEY`. (Alternativ: sätt bara `ADMIN_KEY` och anropa `setup` med fil-id:na – då verifieras filerna och triggern skapas på samma gång; utan `ADMIN_KEY` svarar `setup` `E_SETUP`.)
5. **Prova.** `<URL>?action=ping` ska ge `konfigurerad:true`. Därefter från bokningssidan (`https://redneckengineering.se/bokning?k=<testkod>`) eller från en webbläsarkonsol:
   ```js
   fetch('<URL>', { method:'POST', headers:{ 'Content-Type':'text/plain;charset=utf-8' },
     body: JSON.stringify({ action:'hello', k:'TESTkod_0123456789abcdEF' }) }).then(r => r.json()).then(console.log)
   ```
   Kontrollera efter en testbokning: händelsen i "Bokningar TEST", posten i `telexia-bokning-inbox.json` (`rev` +1) och notismejlet till `notisEpost`. Töm inkorgsfilen och radera testhändelserna efteråt.
6. **Konfigändring under test.** Scriptet cachar config-filen i 10 minuter. Ändras filen manuellt: vänta, eller kör `clearConfigCache()` i redigeraren.
7. **Inkorgen ändrad för hand (version 7).** Inkorgen cachas i CacheService i upp till 6 h (`INBOX_REV`-kontrollen ser bara scriptets egna skrivningar). Redigeras `telexia-bokning-inbox.json` manuellt i Drive: kör `clearSetupCache()` i redigeraren (tömmer även inkorgscachen) – annars kan nästa låsta skrivning (`book`/`ack`/`cancel`/`dailyMaintenance`) skriva över den manuella ändringen med den cachade kopian.

## Felkoder (kortform, spec 4.3)

`E_SETUP` (fil-id/adminnyckel saknas eller filkontroll misslyckas; `details.fil` från `setup`; sedan version 13 även "Kontofilen saknas"/"SESSION_SECRET saknas"), `E_KEY` (okänd/inaktiv/felformaterad kod, eller – sedan version 13 – kod för en bokare som inte är CJ-bokare; alltid samma svar), `E_ADMIN`, `E_RATE` (`details.typ`: `anrop`, `reservation`, `bokningar`, `adresser`, `geocode`, `andringar`, `adressforslag`, sedan version 13 `inloggning`, `mail`), **version 13:** `E_DOMAN` (registrering utan träff i tillåtelselistan), `E_LOGIN` (fel e-post eller lösenord – identiskt svar), `E_OVERIFIERAD` (rätt lösenord men e-posten är inte bekräftad – nytt mail skickat), `E_INAKTIV` (kontot avstängt), `E_SESSION` (sessionen saknas/ogiltig/utgången – logga in igen), `E_TOKEN` (verifierings-/återställningslänk okänd, använd eller utgången), `E_PAUSED`, `E_VALIDATION` (`details.falt`; okänt extrafält rapporteras som `falt.extrafalt` – fältets id ekas aldrig), `E_SLOT_TAKEN`, `E_RESERVATION_EXPIRED`, `E_NOT_FOUND`, `E_STATE` (`details.status`; från `config-push` i stället `details.configRev` när filens rev är äldre än begärd; från `egen-*` med statisk text "Mötet har redan börjat" och `details.passerad:true` när starttiden passerat), `E_CALENDAR`, `E_LOCK`, `E_INTERNAL`, `E_NOT_IMPLEMENTED` (reserverad – ingen endpoint svarar så sedan M5; en äldre scriptdistribution svarar fortfarande `E_NOT_IMPLEMENTED` på `purge`, vilket appen ska tolka som "Uppdatera scriptet").

## Handlingsgränser (konstanter överst i `Code.gs`)

`MAX_RES_PER_KOD` 1 · `MAX_BOOK_PER_KOD_H` 5 · `MAX_BOOK_PER_KOD_D` 15 · `MAX_BOOK_GLOBAL_D` 40 · `MAX_GEOCODE_PER_KOD_H` 20 · `MAX_ADRESSER_PER_KOD_D` 20 · `MAX_ANDR_PER_KOD_H` 5 / `MAX_ANDR_PER_KOD_D` 15 (steg 2a: bokarens egna ändringar – `egen-rebook`/`egen-cancel`/`egen-update` räknas ihop per kod i egna räknare `andr:h`/`andr:d`, `E_RATE` typ `andringar`, plus globalt `MAX_ANDR_GLOBAL_D` 40 per dag i Script Property `andr_count_<YYYYMMDD>` – `ping.bokningarIdag` räknar fortfarande bara nya bokningar; misslyckade anrop räknas inte). `MAX_ADRESSFORSLAG_PER_KOD_M` 60 / `MAX_ADRESSFORSLAG_PER_KOD_D` 600 (steg 2b: `adressforslag` per kod, egna räknare `ac:m`/`ac:d` utanför de allmänna anropsgränserna, `E_RATE` typ `adressforslag`; varje Places-anrop = 1 element mot `MAPS_DAILY_CAP`). Anropsgränser ~30/min och ~300/h per kod (gäller inte `adressforslag`), okända koder 20 per 10 min, admin ~60/min. **Version 13 (konstanter överst i `Konton.gs`):** `KONTO_MAX_LOGIN_FAIL` 8 → spärr 15 min per konto · `KONTO_RL_LOGIN_PER_EPOST_10MIN` 20 · `KONTO_RL_PER_10MIN` 60 (alla `konto-*` globalt) · `KONTO_MAX_LOGIN_FAIL_GLOBAL_D` 300 · `KONTO_MAX_MAIL_D` 30 · `KONTO_MAX_MAIL_PER_KONTO_D` 3 · sessioner 30 d / 12 h · tokens 24 h (verifiera) / 1 h (återställ) · `KONTO_LOSEN_ITER` 400 (2000 tog 1459 ms i Apps Script).

## Loggning

Scriptet loggar en rad per anrop: `{ action, bokareId, ok, code, ms }` – aldrig indata, koder, nycklar eller kundfält. Version 12: `calendar-preview` lägger till `tider: { total, auth, inbox, busy, geo, resor, export, kalla }` och `availability` `tider: { total, busy, geo, restid, plan }` (heltal ms, via `ctx.logg`); triggern loggar `{ trigger:'previewSnap', ok, ms, dagar, handelser, resor, las }`. Vid okontrollerade fel (`E_INTERNAL`) läggs `fel` till: felets namn + en hårt avskalad text (e-postmönster ersatta med `<epost>`, bara ord/siffror/`:.-`, max 80 tecken) – aldrig hela meddelandet, eftersom Calendar/Drive kan eka fältvärden i sina fel. Loggarna finns under Körningar / Cloud Logging i 30 dagar.

## Lås (4.8)

`withScriptLock` (LockService, 10 s) hålls i `reserve`/`book`/`release`/`ack`/`reject`/`rebook`/`cancel`/`purge`/`egen-rebook`/`egen-cancel`/`egen-update` (steg 2a – samma kärnor som `rebook`/`cancel`; `egen-update` utan adressbyte gör bara Drive + `Calendar.Events.patch` under låset), i `setup` (seedning av tomma filer) och vid `mejlfel`-historik; `dailyMaintenance` väntar 20 s (`MAINT_LOCK_WAIT_MS`). `calendar-preview` tar inget lås (bara läsning, `busy:<datum>`-cache 60 s – 11 min när triggern skrivit den, version 12 – och i normalfallet snapshoten `preview:snap`); triggerns förberäkning (`previewSnapKor_`) tar låset bara med `tryLock(0)` för att få fylla inkorgscachen och släpper det direkt efter läsningen; `adressforslag` tar inget lås (CacheService + ett Places-anrop); `block-sync`/blocksynken i `refreshIcsCache` tar inget lås (inkorgen via cache, skrivningar bara i modulens egen kalender) – samtidiga körningar stoppas av CacheService-flaggan `blocksynk:pagar` (`upptagen`), aldrig av `LockService`. All nätverks-I/O (ICS-hämtning, Geocoding, Distance Matrix) görs **före** låset genom att läsa ICS uttryckligen, geokoda adressen och (i `reserve`) köra dagsberäkningen utan färsk kalenderläsning (`warmSlotCaches`; `book` hoppar sedan version 7 över den – `reserve` värmde nyss); ICS-resultatet memoiseras per körning (`KAL_ICS_MEMO`, nollställs i `doPost`) så att den färska läsningen under låset aldrig gör `UrlFetchApp` – även om flödet inte skulle rymmas i CacheService (fler än 8 bitar, se "ICS-cache"). Under låset återstår `Calendar.Events.list` (färsk) + Drive-läsning/skrivning; `book` och `rebook` läser inkorgsfilen exakt en gång under låset (samma objekt skickas vidare till `computeAvailability`/`buildBusyList`). `icsReserv` i cache-filen skrivs högst var 15:e minut. Cache-filen läses högst en gång per körning (`AVAIL_FIL_MEMO`, nollställs i `doPost` av `availResetMemo_`; `updateCacheFile` läser alltid färskt före skrivning) och geokodningar memoiseras per körning (`AVAIL_GEO_MEMO` – bär även place_id-resultatet under den skrivna adressen inom körningen, se "`placeId` i geokodningen") och nya geokod- och restidsposter skrivs samlat en gång per körning (`AVAIL_GEOKOD_PENDING` + `AVAIL_RESTID_PENDING` → `availFlushGeokod_` i `doPost`/`dailyMaintenance`/`refreshIcsCache` efter handlern, utanför låset) – A51/version 7. Inkorgen läses ur CacheService (version 7, `inbox:<fileId>`) och skrivs alltid under lås (invalidera → Drive → ny cache + `INBOX_REV`); cachen fylls bara av låshållaren (`LAS_HALLS_`) och en träff godtas bara med rätt `rev` – se "Inkorgen i CacheService". `updateCacheFile` i Availability.gs tar **inget** lås – nästla aldrig `withScriptLock` (reentrans i LockService är inte dokumenterad).
